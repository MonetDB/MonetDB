# "Meta-Makefile[.in]" for buildtools

SRC="@SRC@"
BUILD="@BUILD@"
PREFIX="@PREFIX@"

# Run individual "make [all]"s ...
default: all
	@-echo

all:
	cd $(SRC)/autogen && python setup.py build
	cd _MX && $(MAKE) $@
	cd _MEL && $(MAKE) $@
	cd _BURG && $(MAKE) $@

# ... run individual "make install"s ...
install:
	cd $(SRC)/autogen && python setup.py install --prefix=$(PREFIX)
	cd _MX && $(MAKE) $@
	cd _MEL && $(MAKE) $@
	cd _BURG && $(MAKE) $@

# ... and individual "make *"s
%:
	cd _MX && $(MAKE) $@
	cd _MEL && $(MAKE) $@
	cd _BURG && $(MAKE) $@
