barrier (v1,i):= language.newRange(0);
	io.print("do something");
	redo (v1,i):= language.nextElement(1,2);
exit	(v1,i);
	io.printf("left first loop with %d\n",v1);
	io.print("next iteration");
barrier (v2,i):= language.newRange(0);
	io.print("do something else");
	redo (v2,i):= language.nextElement(2,5);
exit	(v2,i);
