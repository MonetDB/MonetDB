# parsing operations with multi-assignments
function swap(x:bat[:any_1,:int],y:bat[:int,:int]) 
	(a:str,b:bat[:any_1,:int]);
        a:="done"; 
        return (a,x);
end swap;

X:= bat.new(:int,:int);
bat.insert(X,1,1);
Y:= bat.new(:int,:int);
bat.insert(Y,2,2);
(x,y):= user.swap(X,Y);
io.print(x);
io.print(y);

