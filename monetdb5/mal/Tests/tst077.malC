# parsing operations with multi-assignments
function swap(x:bat[:oid,:int],y:bat[:oid,:int]) (a:str,b:bat[:oid,:int]);
        a:="done"; 
        return (a,x);
end swap;

function foo();
X:= bat.new(:oid,:int);
bat.append(X,1);
Y:= bat.new(:oid,:int);
bat.append(Y,2);
(x,y):= user.swap(X,Y);
io.print(x);
io.print(y);
end foo;

mdb.List("user","foo");
user.foo();

