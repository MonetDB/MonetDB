# Compression of noncompressable column


b:= bat.new(:int);
bat.append(b,50);
bat.append(b,19531015);
bat.append(b,9);
b:= bat.append(b,b);
base:= algebra.copy(b);

io.print(base);
mosaic.compress(b,"raw");
#mosaic.dump(b);
s:= algebra.subselect(base,0,50,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,0,50,false,false,false);
io.print(xs);

s:= algebra.subselect(base,0,50,false,true,false);
io.print(s);
xs:= mosaic.subselect(b,0,50,false,true,false);
io.print(xs);

s:= algebra.subselect(base,0,50,true,false,false);
io.print(s);
xs:= mosaic.subselect(b,0,50,true,false,false);
io.print(xs);

s:= algebra.subselect(base,0,50,true,true,false);
io.print(s);
xs:= mosaic.subselect(b,0,50,true,true,false);
io.print(xs);

s:= algebra.subselect(base,nil:int,50,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,nil:int,50,false,false,false);
io.print(xs);

s:= algebra.subselect(base,50,nil:int,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,50,nil:int,false,false,false);
io.print(xs);

s:= algebra.subselect(base,nil:int,nil:int,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,nil:int,nil:int,false,false,false);
io.print(xs);

s:= algebra.subselect(base,nil:int,nil:int,false,false,true);
io.print(s);
xs:= mosaic.subselect(b,nil:int,nil:int,false,false,true);
io.print(xs);

# using candidatelists
io.print("candidate stuff");
c:= bat.new(:oid);
bat.append(c,2@0);
bat.append(c,5@0);

s:= algebra.subselect(base,c,0,50,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,c,0,50,false,false,false);
io.print(xs);

s:= algebra.subselect(base,c,0,50,false,true,false);
io.print(s);
xs:= mosaic.subselect(b,c,0,50,false,true,false);
io.print(xs);

s:= algebra.subselect(base,c,0,50,true,false,false);
io.print(s);
xs:= mosaic.subselect(b,c,0,50,true,false,false);
io.print(xs);

s:= algebra.subselect(base,c,0,50,true,true,false);
io.print(s);
xs:= mosaic.subselect(b,c,0,50,true,true,false);
io.print(xs);

s:= algebra.subselect(base,c,nil:int,50,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,c,nil:int,50,false,false,false);
io.print(xs);

s:= algebra.subselect(base,c,50,nil:int,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,c,50,nil:int,false,false,false);
io.print(xs);

s:= algebra.subselect(base,c,nil:int,nil:int,false,false,false);
io.print(s);
xs:= mosaic.subselect(b,c,nil:int,nil:int,false,false,false);
io.print(xs);

s:= algebra.subselect(base,c,nil:int,nil:int,false,false,true);
io.print(s);
xs:= mosaic.subselect(b,c,nil:int,nil:int,false,false,true);
io.print(xs);


io.print("string stuff");
bstr:= bat.new(:str);
bat.append(bstr,"the");
bat.append(bstr,"big");
bat.append(bstr,"brown");
bat.append(bstr,"fox");

ystr:= algebra.copy(bstr);
mosaic.compress(ystr,"raw");

tstr:= algebra.subselect(bstr,"big","fox",true,true,false);
io.print(tstr);
tstr:= mosaic.subselect(ystr,"big","fox",true,true,false);
io.print(tstr);
