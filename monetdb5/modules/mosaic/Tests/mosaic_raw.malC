b:= bat.new(:int);
bat.append(b,50);
bat.append(b,19531015);
bat.append(b,9);
b:= bat.append(b,b);
base:= algebra.copy(b);

io.print(b);
mosaic.compress(b,"raw");
#mosaic.dump(b);
mosaic.decompress(b);
io.print(b);


s:= bat.new(:str);
bat.append(s,"the");
bat.append(s,"big");
bat.append(s,"brown");
bat.append(s,"fox");

mosaic.compress(s,"raw");
mosaic.decompress(s);
io.print(s);
