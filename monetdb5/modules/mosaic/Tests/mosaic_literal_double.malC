b:= bat.new(:int);
bat.append(b,50);
bat.append(b,19531015);
bat.append(b,9);
b:= bat.append(b,b);
base:= algebra.copy(b);

io.print(base);
x:= mosaic.compress(b,"literal");
mosaic.dump(x);
xd := mosaic.decompress(x);
io.print(xd);

z:= mosaic.compress(x,"literal");
mosaic.dump(z);
zd := mosaic.decompress(z);
io.print(zd);
