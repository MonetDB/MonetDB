a:= bat.new(:lng);
bat.append(a,1000:lng);
bat.append(a,1001:lng);
bat.append(a,999:lng);
bat.append(a,1002:lng);
bat.append(a,998:lng);
bat.append(a,1010:lng);
bat.append(a,990:lng);
bat.append(a,995:lng);
bat.append(a,1005:lng);
bat.append(a,1002:lng);
bat.append(a,1000:lng);
bat.append(a,1001:lng);
bat.append(a,999:lng);
bat.append(a,1002:lng);
bat.append(a,9223372036854775800:lng);
bat.append(a,1000:lng);
bat.append(a,1000:lng);
bat.append(a,1000:lng);
bat.append(a,1000:lng);
bat.append(a,1001:lng);
bat.append(a,1001:lng);
bat.append(a,1001:lng);
bat.append(a,1001:lng);
bat.append(a,1002:lng);
bat.append(a,1002:lng);
bat.append(a,1002:lng);
bat.append(a,1002:lng);
bat.append(a,998:lng);
bat.append(a,998:lng);
bat.append(a,998:lng);
bat.append(a,998:lng);
bat.append(a,999:lng);
bat.append(a,999:lng);
bat.append(a,999:lng);
bat.append(a,999:lng);
bat.append(a,1002:lng);
bat.append(a,1002:lng);
bat.append(a,1002:lng);
bat.append(a,1002:lng);
bat.append(a,1010:lng);
bat.append(a,1010:lng);
bat.append(a,1010:lng);
bat.append(a,1010:lng);
bat.append(a,990:lng);
bat.append(a,990:lng);
bat.append(a,990:lng);
bat.append(a,990:lng);
bat.append(a,995:lng);
bat.append(a,995:lng);
bat.append(a,995:lng);
bat.append(a,995:lng);
bat.append(a,1005:lng);
bat.append(a,1005:lng);
bat.append(a,1005:lng);
bat.append(a,1005:lng);
bat.append(a,998:lng);
bat.append(a,1010:lng);
bat.append(a,990:lng);
bat.append(a,995:lng);
bat.append(a,1005:lng);
bat.append(a,9223372036854775800:lng);
bat.append(a,1002:lng);
bat.append(a,1000:lng);
bat.append(a,1001:lng);
bat.append(a,999:lng);
bat.append(a,1002:lng);
bat.append(a,998:lng);
bat.append(a,1010:lng);
bat.append(a,990:lng);
bat.append(a,995:lng);
bat.append(a,1005:lng);
bat.append(a,1002:lng);
mosaic.compress(a,"{compression_type}");
c:= mosaic.decompress(a);
eq  := batcalc.==(a,  c, true:bit);
io.print(a, c, eq);

b:= bat.new(:lng);
bat.append(b,1000:lng);
bat.append(b,1001:lng);
bat.append(b,999:lng);
bat.append(b,nil:lng);
bat.append(b,1003:lng);
bat.append(b,997:lng);
bat.append(b,1010:lng);
bat.append(b,nil:lng);
bat.append(b,nil:lng);
bat.append(b,nil:lng);
bat.append(b,991:lng);
bat.append(b,992:lng);
bat.append(b,993:lng);
bat.append(b,1000:lng);


#materialized candidate list for column b
mcl_b:= bat.new(:oid);
bat.append(mcl_b,2:oid);
bat.append(mcl_b,4:oid);
bat.append(mcl_b,6:oid);
bat.append(mcl_b,8:oid);
bat.append(mcl_b,10:oid);
bat.append(mcl_b,12:oid);
bat.append(mcl_b,14:oid);
bat.append(mcl_b,16:oid);
bat.append(mcl_b,18:oid);
bat.append(mcl_b,20:oid);

#dense candidate list for column b
dcl_b := bat.densebat(6:lng);

#materialized candidate list for columns a/c
mcl_a:= bat.new(:oid);
bat.append(mcl_a,2:oid);
bat.append(mcl_a,4:oid);
bat.append(mcl_a,6:oid);
bat.append(mcl_a,8:oid);
bat.append(mcl_a,10:oid);
bat.append(mcl_a,12:oid);
bat.append(mcl_a,14:oid);
bat.append(mcl_a,16:oid);
bat.append(mcl_a,18:oid);
bat.append(mcl_a,20:oid);
bat.append(mcl_a,22:oid);
bat.append(mcl_a,24:oid);
bat.append(mcl_a,26:oid);
bat.append(mcl_a,28:oid);
bat.append(mcl_a,30:oid);
bat.append(mcl_a,32:oid);
bat.append(mcl_a,34:oid);
bat.append(mcl_a,36:oid);
bat.append(mcl_a,38:oid);
bat.append(mcl_a,40:oid);
bat.append(mcl_a,42:oid);
bat.append(mcl_a,44:oid);
bat.append(mcl_a,46:oid);
bat.append(mcl_a,48:oid);
bat.append(mcl_a,50:oid);
bat.append(mcl_a,52:oid);
bat.append(mcl_a,54:oid);
bat.append(mcl_a,56:oid);
bat.append(mcl_a,58:oid);
bat.append(mcl_a,60:oid);
bat.append(mcl_a,62:oid);
bat.append(mcl_a,64:oid);
bat.append(mcl_a,66:oid);
bat.append(mcl_a,68:oid);

# partially covering dense set.
dcl_a := bat.densebat(35:lng);

size_a:lng := aggr.count(a);
# fully covering dense set.
fdcl_a := bat.densebat(size_a);

# projection with materialized candidate list
tsap    := algebra.projection(mcl_a, a);
tsapc   := mosaic.projection(mcl_a, c);
eq  := batcalc.==(tsap, tsapc, true:bit);
io.print(eq);

# projection with dense candidate list
tsap    := algebra.projection(dcl_a, a);
tsapc   := mosaic.projection(dcl_a, c);
eq  := batcalc.==(tsap, tsapc, true:bit);
io.print(eq);

# projection with fully covering dense candidate list
sap    := algebra.projection(fdcl_a, a);
sapc   := mosaic.projection(fdcl_a, c);
eq  := batcalc.==(sap, sapc, true:bit);
io.print(eq);

# (theta)selects and joins without candidate list for columns a/c.

sa:= algebra.select(a,nil:lng, nil:lng,false,false,false);
sc:= mosaic.select (c,nil:lng, nil:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,nil:lng, nil:lng,false,false,true);
sc:= mosaic.select (c,nil:lng, nil:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1000:lng, nil:lng,false,false,false);
sc:= mosaic.select (c,1000:lng, nil:lng,false,false,false);
tsa:= algebra.thetaselect(a,1000:lng, ">");
tsc:= mosaic.thetaselect (c,1000:lng, ">");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,nil:lng, 1000:lng,false,false,false);
sc:= mosaic.select (c,nil:lng, 1000:lng,false,false,false);
tsa:= algebra.thetaselect(a,1000:lng, "<");
tsc:= mosaic.thetaselect (c,1000:lng, "<");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,900:lng, nil:lng,false,false,false);
sc:= mosaic.select (c,900:lng, nil:lng,false,false,false);
tsa:= algebra.thetaselect(a,900:lng, ">");
tsc:= mosaic.thetaselect (c,900:lng, ">");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,nil:lng, 900:lng,false,false,false);
sc:= mosaic.select (c,nil:lng, 900:lng,false,false,false);
tsa:= algebra.thetaselect(a,900:lng, "<");
tsc:= mosaic.thetaselect (c,900:lng, "<");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,900:lng, nil:lng,true,false,false);
sc:= mosaic.select (c,900:lng, nil:lng,true,false,false);
tsa:= algebra.thetaselect(a,900:lng, ">=");
tsc:= mosaic.thetaselect (c,900:lng, ">=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,nil:lng, 900:lng,false,true,false);
sc:= mosaic.select (c,nil:lng, 900:lng,false,true,false);
tsa:= algebra.thetaselect(a,900:lng, "<=");
tsc:= mosaic.thetaselect (c,900:lng, "<=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1000:lng, nil:lng,true,false,false);
sc:= mosaic.select (c,1000:lng, nil:lng,true,false,false);
tsa:= algebra.thetaselect(a,1000:lng, ">=");
tsc:= mosaic.thetaselect (c,1000:lng, ">=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,nil:lng, 1000:lng,false,true,false);
sc:= mosaic.select (c,nil:lng, 1000:lng,false,true,false);
tsa:= algebra.thetaselect(a,1000:lng, "<=");
tsc:= mosaic.thetaselect (c,1000:lng, "<=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1000:lng, 1000:lng,true,true,false);
sc:= mosaic.select (c,1000:lng, 1000:lng,true,true,false);
tsa:= algebra.thetaselect(a,1000:lng, "==");
tsc:= mosaic.thetaselect (c,1000:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1000:lng, 1000:lng,true,true,true);
sc:= mosaic.select (c,1000:lng, 1000:lng,true,true,true);
tsa:= algebra.thetaselect(a,1000:lng, "!=");
tsc:= mosaic.thetaselect (c,1000:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1003:lng, 1003:lng,true,true,false);
sc:= mosaic.select (c,1003:lng, 1003:lng,true,true,false);
tsa:= algebra.thetaselect(a,1003:lng, "==");
tsc:= mosaic.thetaselect (c,1003:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1003:lng, 1003:lng,true,true,true);
sc:= mosaic.select (c,1003:lng, 1003:lng,true,true,true);
tsa:= algebra.thetaselect(a,1003:lng, "!=");
tsc:= mosaic.thetaselect (c,1003:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,900:lng, 900:lng,true,true,false);
sc:= mosaic.select (c,900:lng, 900:lng,true,true,false);
tsa:= algebra.thetaselect(a,900:lng, "==");
tsc:= mosaic.thetaselect (c,900:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,900:lng, 900:lng,true,true,true);
sc:= mosaic.select (c,900:lng, 900:lng,true,true,true);
tsa:= algebra.thetaselect(a,900:lng, "!=");
tsc:= mosaic.thetaselect (c,900:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1100:lng, 1100:lng,true,true,false);
sc:= mosaic.select (c,1100:lng, 1100:lng,true,true,false);
tsa:= algebra.thetaselect(a,1100:lng, "==");
tsc:= mosaic.thetaselect (c,1100:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a,1100:lng, 1100:lng,true,true,true);
sc:= mosaic.select (c,1100:lng, 1100:lng,true,true,true);
tsa:= algebra.thetaselect(a,1100:lng, "!=");
tsc:= mosaic.thetaselect (c,1100:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

# Should give empty result
sa:= algebra.select(a,1000:lng, 1000:lng,false,true,false);
sc:= mosaic.select (c,1000:lng, 1000:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1000:lng, 1000:lng,true,false,false);
sc:= mosaic.select (c,1000:lng, 1000:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1000:lng, 1000:lng,false,false,false);
sc:= mosaic.select (c,1000:lng, 1000:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

# Should give all non-nil value
sa:= algebra.select(a,1000:lng, 1000:lng,false,true,true);
sc:= mosaic.select (c,1000:lng, 1000:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1000:lng, 1000:lng,true,false,true);
sc:= mosaic.select (c,1000:lng, 1000:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1000:lng, 1000:lng,false,false,true);
sc:= mosaic.select (c,1000:lng, 1000:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1005:lng, 991:lng,true,true,false);
sc:= mosaic.select (c,1005:lng, 991:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,1005:lng, 991:lng,true,true,true);
sc:= mosaic.select (c,1005:lng, 991:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c,991:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c,999:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c,991:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c,999:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c,991:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c,999:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c,991:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c,999:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c,991:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c,999:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c,991:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c,999:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c,991:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c,999:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c,991:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c,999:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c,991:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c,999:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c,991:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c,999:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c,991:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c,999:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c,991:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c,999:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c,991:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c,999:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c,991:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c,999:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c,991:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c,999:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c,991:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c,999:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c,980:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c,980:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c,980:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c,999:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,991:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,991:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,991:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,999:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,991:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,999:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,991:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,999:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,991:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,999:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,991:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,999:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,991:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,999:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,991:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,999:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,991:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,999:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,991:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,999:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,991:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,999:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,991:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,999:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,991:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,999:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,991:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,999:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,991:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,991:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,999:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

# joins with no candidate list for column b

(l,r)       := algebra.join(a,b,nil:bat,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,nil:bat, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,nil:bat, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,nil:bat, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,nil:bat, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,nil:bat, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,nil:bat, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,nil:bat, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,nil:bat, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# joins with materialized candidate list for column b

(l,r)       := algebra.join(a,b,nil:bat,mcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,mcl_b, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,mcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,mcl_b, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,nil:bat, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,nil:bat, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,mcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,mcl_b, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,mcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,mcl_b, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,nil:bat, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,nil:bat, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# joins with dense candidate list for column b

(l,r)       := algebra.join(a,b,nil:bat,dcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,dcl_b, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,dcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,dcl_b, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,nil:bat, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,nil:bat, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,dcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,dcl_b, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,nil:bat,dcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,nil:bat,dcl_b, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,nil:bat, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,nil:bat, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# (theta)selects and joins with materialized candidate list for columns a/c.

sa:= algebra.select(a, mcl_a,nil:lng, nil:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,nil:lng, nil:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,nil:lng, nil:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,nil:lng, nil:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1000:lng, nil:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,1000:lng, nil:lng,false,false,false);
tsa:= algebra.thetaselect(a, mcl_a,1000:lng, ">");
tsc:= mosaic.thetaselect (c, mcl_a,1000:lng, ">");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,nil:lng, 1000:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,nil:lng, 1000:lng,false,false,false);
tsa:= algebra.thetaselect(a, mcl_a,1000:lng, "<");
tsc:= mosaic.thetaselect (c, mcl_a,1000:lng, "<");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,900:lng, nil:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,900:lng, nil:lng,false,false,false);
tsa:= algebra.thetaselect(a, mcl_a,900:lng, ">");
tsc:= mosaic.thetaselect (c, mcl_a,900:lng, ">");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,nil:lng, 900:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,nil:lng, 900:lng,false,false,false);
tsa:= algebra.thetaselect(a, mcl_a,900:lng, "<");
tsc:= mosaic.thetaselect (c, mcl_a,900:lng, "<");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,900:lng, nil:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,900:lng, nil:lng,true,false,false);
tsa:= algebra.thetaselect(a, mcl_a,900:lng, ">=");
tsc:= mosaic.thetaselect (c, mcl_a,900:lng, ">=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,nil:lng, 900:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,nil:lng, 900:lng,false,true,false);
tsa:= algebra.thetaselect(a, mcl_a,900:lng, "<=");
tsc:= mosaic.thetaselect (c, mcl_a,900:lng, "<=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1000:lng, nil:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,1000:lng, nil:lng,true,false,false);
tsa:= algebra.thetaselect(a, mcl_a,1000:lng, ">=");
tsc:= mosaic.thetaselect (c, mcl_a,1000:lng, ">=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,nil:lng, 1000:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,nil:lng, 1000:lng,false,true,false);
tsa:= algebra.thetaselect(a, mcl_a,1000:lng, "<=");
tsc:= mosaic.thetaselect (c, mcl_a,1000:lng, "<=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,true,true,false);
tsa:= algebra.thetaselect(a, mcl_a,1000:lng, "==");
tsc:= mosaic.thetaselect (c, mcl_a,1000:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,true,true,true);
tsa:= algebra.thetaselect(a, mcl_a,1000:lng, "!=");
tsc:= mosaic.thetaselect (c, mcl_a,1000:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1003:lng, 1003:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,1003:lng, 1003:lng,true,true,false);
tsa:= algebra.thetaselect(a, mcl_a,1003:lng, "==");
tsc:= mosaic.thetaselect (c, mcl_a,1003:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1003:lng, 1003:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,1003:lng, 1003:lng,true,true,true);
tsa:= algebra.thetaselect(a, mcl_a,1003:lng, "!=");
tsc:= mosaic.thetaselect (c, mcl_a,1003:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,900:lng, 900:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,900:lng, 900:lng,true,true,false);
tsa:= algebra.thetaselect(a, mcl_a,900:lng, "==");
tsc:= mosaic.thetaselect (c, mcl_a,900:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,900:lng, 900:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,900:lng, 900:lng,true,true,true);
tsa:= algebra.thetaselect(a, mcl_a,900:lng, "!=");
tsc:= mosaic.thetaselect (c, mcl_a,900:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1100:lng, 1100:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,1100:lng, 1100:lng,true,true,false);
tsa:= algebra.thetaselect(a, mcl_a,1100:lng, "==");
tsc:= mosaic.thetaselect (c, mcl_a,1100:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, mcl_a,1100:lng, 1100:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,1100:lng, 1100:lng,true,true,true);
tsa:= algebra.thetaselect(a, mcl_a,1100:lng, "!=");
tsc:= mosaic.thetaselect (c, mcl_a,1100:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

# Should give empty result
sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

# Should give all non-nil value
sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1000:lng, 1000:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,1000:lng, 1000:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1005:lng, 991:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,1005:lng, 991:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,1005:lng, 991:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,1005:lng, 991:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,991:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,991:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, mcl_a,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, mcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, mcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

# joins with no candidate list for column b

(l,r)       := algebra.join(a,b,mcl_a,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,nil:bat, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,nil:bat, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,mcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,mcl_a, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,mcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,mcl_a, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,nil:bat, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,nil:bat, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,mcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,mcl_a, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,mcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,mcl_a, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# joins with materialized candidate list for column b

(l,r)       := algebra.join(a,b,mcl_a,mcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,mcl_b, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,mcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,mcl_b, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,mcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,mcl_a, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,mcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,mcl_a, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,mcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,mcl_b, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,mcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,mcl_b, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,mcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,mcl_a, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,mcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,mcl_a, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# joins with dense candidate list for column b

(l,r)       := algebra.join(a,b,mcl_a,dcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,dcl_b, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,dcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,dcl_b, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,mcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,mcl_a, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,mcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,mcl_a, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,dcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,dcl_b, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,mcl_a,dcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,mcl_a,dcl_b, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,mcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,mcl_a, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,mcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,mcl_a, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# (theta)selects and joins with dense candidate list for columns a/c.

sa:= algebra.select(a, dcl_a,nil:lng, nil:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,nil:lng, nil:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,nil:lng, nil:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,nil:lng, nil:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1000:lng, nil:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,1000:lng, nil:lng,false,false,false);
tsa:= algebra.thetaselect(a, dcl_a,1000:lng, ">");
tsc:= mosaic.thetaselect (c, dcl_a,1000:lng, ">");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,nil:lng, 1000:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,nil:lng, 1000:lng,false,false,false);
tsa:= algebra.thetaselect(a, dcl_a,1000:lng, "<");
tsc:= mosaic.thetaselect (c, dcl_a,1000:lng, "<");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,900:lng, nil:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,900:lng, nil:lng,false,false,false);
tsa:= algebra.thetaselect(a, dcl_a,900:lng, ">");
tsc:= mosaic.thetaselect (c, dcl_a,900:lng, ">");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,nil:lng, 900:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,nil:lng, 900:lng,false,false,false);
tsa:= algebra.thetaselect(a, dcl_a,900:lng, "<");
tsc:= mosaic.thetaselect (c, dcl_a,900:lng, "<");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,900:lng, nil:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,900:lng, nil:lng,true,false,false);
tsa:= algebra.thetaselect(a, dcl_a,900:lng, ">=");
tsc:= mosaic.thetaselect (c, dcl_a,900:lng, ">=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,nil:lng, 900:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,nil:lng, 900:lng,false,true,false);
tsa:= algebra.thetaselect(a, dcl_a,900:lng, "<=");
tsc:= mosaic.thetaselect (c, dcl_a,900:lng, "<=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1000:lng, nil:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,1000:lng, nil:lng,true,false,false);
tsa:= algebra.thetaselect(a, dcl_a,1000:lng, ">=");
tsc:= mosaic.thetaselect (c, dcl_a,1000:lng, ">=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,nil:lng, 1000:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,nil:lng, 1000:lng,false,true,false);
tsa:= algebra.thetaselect(a, dcl_a,1000:lng, "<=");
tsc:= mosaic.thetaselect (c, dcl_a,1000:lng, "<=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,true,true,false);
tsa:= algebra.thetaselect(a, dcl_a,1000:lng, "==");
tsc:= mosaic.thetaselect (c, dcl_a,1000:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,true,true,true);
tsa:= algebra.thetaselect(a, dcl_a,1000:lng, "!=");
tsc:= mosaic.thetaselect (c, dcl_a,1000:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1003:lng, 1003:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,1003:lng, 1003:lng,true,true,false);
tsa:= algebra.thetaselect(a, dcl_a,1003:lng, "==");
tsc:= mosaic.thetaselect (c, dcl_a,1003:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1003:lng, 1003:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,1003:lng, 1003:lng,true,true,true);
tsa:= algebra.thetaselect(a, dcl_a,1003:lng, "!=");
tsc:= mosaic.thetaselect (c, dcl_a,1003:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,900:lng, 900:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,900:lng, 900:lng,true,true,false);
tsa:= algebra.thetaselect(a, dcl_a,900:lng, "==");
tsc:= mosaic.thetaselect (c, dcl_a,900:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,900:lng, 900:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,900:lng, 900:lng,true,true,true);
tsa:= algebra.thetaselect(a, dcl_a,900:lng, "!=");
tsc:= mosaic.thetaselect (c, dcl_a,900:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1100:lng, 1100:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,1100:lng, 1100:lng,true,true,false);
tsa:= algebra.thetaselect(a, dcl_a,1100:lng, "==");
tsc:= mosaic.thetaselect (c, dcl_a,1100:lng, "==");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

sa:= algebra.select(a, dcl_a,1100:lng, 1100:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,1100:lng, 1100:lng,true,true,true);
tsa:= algebra.thetaselect(a, dcl_a,1100:lng, "!=");
tsc:= mosaic.thetaselect (c, dcl_a,1100:lng, "!=");
eq1  := batcalc.==(sa,  sc);
eq2  := batcalc.==(tsa, tsc);
eq3  := batcalc.==(sc, tsc);
io.print(sa, sc, tsa, tsc, eq1, eq2, eq3);

# Should give empty result
sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

# Should give all non-nil value
sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1000:lng, 1000:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,1000:lng, 1000:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1005:lng, 991:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,1005:lng, 991:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,1005:lng, 991:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,1005:lng, 991:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1008:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1008:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1005:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1005:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1008:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1008:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,991:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,991:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,999:lng, 1020:lng,true,true,false);
sc:= mosaic.select (c, dcl_a,999:lng, 1020:lng,true,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,false);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,false);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,true,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,true,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,true,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,true,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

sa:= algebra.select(a, dcl_a,980:lng, 1020:lng,false,false,true);
sc:= mosaic.select (c, dcl_a,980:lng, 1020:lng,false,false,true);
eq  := batcalc.==(sa,  sc);
io.print(sa, sc, eq);

# joins with no candidate list for column b

(l,r)       := algebra.join(a,b,dcl_a,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,nil:bat, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,nil:bat, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,dcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,dcl_a, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,dcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,dcl_a, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,nil:bat, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,nil:bat, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,nil:bat, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,nil:bat, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,dcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,dcl_a, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,nil:bat,dcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,nil:bat,dcl_a, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# joins with materialized candidate list for column b

(l,r)       := algebra.join(a,b,dcl_a,mcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,mcl_b, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,mcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,mcl_b, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,dcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,dcl_a, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,dcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,dcl_a, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,mcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,mcl_b, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,mcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,mcl_b, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,dcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,dcl_a, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,mcl_b,dcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,mcl_b,dcl_a, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

# joins with dense candidate list for column b

(l,r)       := algebra.join(a,b,dcl_a,dcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,dcl_b, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,dcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,dcl_b, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,dcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,dcl_a, false, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,dcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,dcl_a, true, 0:lng);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,dcl_b, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,dcl_b, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(a,b,dcl_a,dcl_b, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (c,b,dcl_a,dcl_b, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,dcl_a, false, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,dcl_a, false, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);

(l,r)       := algebra.join(b, a,dcl_b,dcl_a, true, 0:lng);
(r, o, g)   := algebra.sort(r, false:bit, false:bit, false:bit);
(l, o, g)   := algebra.sort(l, o, g, false:bit, false:bit, false:bit);

(lc,rc)     := mosaic.join (b, c,dcl_b,dcl_a, true, 0:lng, true:bit);
(rc, o, g)  := algebra.sort(rc, false:bit, false:bit, false:bit);
(lc, o, g)  := algebra.sort(lc, o, g, false:bit, false:bit, false:bit);
eq  := batcalc.==(l, lc, true:bit);
io.print(l, lc, eq);
eq  := batcalc.==(r, rc, true:bit);
io.print(r, rc, eq);
