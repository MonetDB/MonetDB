    V7 := bat.new(:oid,:oid);
    V10 := bat.new(:oid,:oid);
    V16 := algebra.mark(V7);
    V17 := algebra.join(V16,V7);
    V19 := bat.new(:oid,:oid);
    V22 := bat.new(:oid,:oid);
    V23 := algebra.join(V16,V22);
	io.print("done");

optimizer.deadcode();
mdb.List();
