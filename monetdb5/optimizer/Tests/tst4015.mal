# testing repeatative reverse optimalisation
r:= bat.new(:int,:int);
bat.insert(r,5:int,1:int);
o:= 0:oid;
rr:= bat.reverse(r);
z:= algebra.markT(rr,o);
s:= bat.reverse(r);
t:= bat.reverse(s);
k:=23:int;
io.print(t);
optimizer.peephole();
