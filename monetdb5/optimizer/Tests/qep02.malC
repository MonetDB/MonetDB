b:= true;
T1:= bat.new(:oid);
T3:= bat.new(:oid);
barrier b;
	a:= algebra.select(T1,nil:bat[:oid],1@0,3@0,true,true,false);
	c:= algebra.select(T3,nil:bat[:oid],0@0,5@0,true,true,false);
	(e,f):= algebra.join(a,c,nil:bat,nil:bat,false,nil:lng);
exit b;
optimizer.dumpQEP();
