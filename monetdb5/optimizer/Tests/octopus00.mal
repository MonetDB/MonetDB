function qry();
	b{hlb=0@0,hub=100@0}:= bat.new(:oid,:int);
	bat.append(b,1);
	bat.append(b,2);
	bat.append(b,3);
	bat.append(b,4);
	(b1:bat[:oid,:int],b2:bat[:oid,:int]):= bat.partition(b);
	z:= mat.new(b1,b2);
	s:= algebra.select(z,0,20);
	io.print(s);
end qry;
optimizer.mergetable("user","qry");
optimizer.octopus("user","qry");
optimizer.deadcode("user","qry");
mdb.list("user","qry");
mdb.list("octopus","exec_qry");
mdb.list("octopus","tentacle_qry_21");
mdb.list("octopus","tentacle_qry_22");

io.print("try it, it won't produce results, because we don't ship the bat");
user.qry();
