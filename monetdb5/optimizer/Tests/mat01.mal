# the Multi Assocation Tables
# use two mats at the same time.
	mdb.List();
    m0:= bat.new(:oid,:int);
    bat.insert(m0,nil:oid,0);
    m1:= bat.new(:oid,:int);
    bat.insert(m1,nil:oid,1);
    m2:= bat.new(:oid,:int);
    bat.insert(m2,nil:oid,2);
    b := mat.new(m0,m1,m2);
	d := mat.new(m1,m2);
    s := algebra.select(b,0,2);
    i := aggr.count(s);
    io.print(s);
    io.print(i);
    c0 := bat.new(:int,:int);
    bat.insert(c0,0,4);
    c1 := bat.new(:int,:int);
    bat.insert(c1,1,5);
    c := mat.new(c0,c1);
    bat.insert(c,1,6);
    j := algebra.join(b,c);
    io.print(j);
    s := algebra.select(d,0,2);
    i := aggr.count(s);
    io.print(s);
io.print(b);
io.print(c);
io.print(d);

optimizer.mergetable();
