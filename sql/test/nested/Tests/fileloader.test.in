statement ok
create type event as (id int, type varchar)

query IT
select t.id, t.type from ( select cast(t.json as event) as t from (select json from r'$TSTSRCDIR/events.json') t );
----
1
click
2
scroll

query T
select cast(t.json as event) from (select json from r'$TSTSRCDIR/events.json') t
----
(1, "click")
(2, "scroll")

statement ok
drop type event
