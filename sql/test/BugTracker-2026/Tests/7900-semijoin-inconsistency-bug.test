statement ok
CREATE SCHEMA s7900

statement ok
SET SCHEMA s7900

statement ok
CREATE TABLE comments (
    id          INT,
    post_id     INT,
    user_id     INT,
    content     VARCHAR(1000),
    is_spam     INT,
    created_at  TIMESTAMP
)

statement ok
CREATE TABLE orders (
    id          INT,
    user_id     INT,
    amount      DOUBLE,
    status      VARCHAR(20),
    created_at  TIMESTAMP
)

statement ok rowcount 4
INSERT INTO comments VALUES
(1, 1, 2, 'Nice post', 0, '2022-01-20 10:00:00'),
(2, 1, 3, 'Spam here', 1,  '2022-01-21 11:00:00'),
(3, 2, 1, 'Thanks',    0, '2022-01-22 12:00:00'),
(4, 4, 5, NULL,        0, '2022-01-23 13:00:00')

statement ok rowcount 5
INSERT INTO orders VALUES
(1, 1, 100.00, 'paid',    '2022-02-01 09:00:00'),
(2, 1, 200.50, 'shipped', '2022-02-02 10:00:00'),
(3, 2, NULL,   'failed',  '2022-02-03 11:00:00'),
(4, 3, 50.00,  'paid',    '2022-02-04 12:00:00'),
(5, 5, 999.99, 'paid',    '2022-02-05 13:00:00')


-- RESULT: {0}
query I
SELECT COUNT(*)
FROM (
    SELECT ref_1.id AS c0
    FROM comments AS ref_0
    LEFT JOIN comments AS ref_1
        ON ref_0.id > ref_1.id
    INNER JOIN comments AS ref_2
        ON (
            (SELECT AVG(id) FROM orders) != 10.28
            OR ref_1.is_spam != ref_2.is_spam
        )
    WHERE CASE
            WHEN 64.13 = (SELECT AVG(id) FROM comments) THEN 60.96
            ELSE CASE
                    WHEN ref_0.post_id < ref_2.user_id THEN 61.49
                    ELSE 30.99
                 END
          END >= 21.42
) AS subq_0
WHERE (
        (FALSE OR FALSE)
        AND NULLIF(54.17, 65.41) >= (SELECT STDDEV_POP(id) FROM comments)
      )
   OR subq_0.c0 > subq_0.c0
----
0

-- RESULT: {28}
query I
SELECT SUM(case when ((
        (FALSE OR FALSE)
        AND NULLIF(54.17, 65.41) >= (SELECT STDDEV_POP(id) FROM comments)
      )
   OR subq_0.c0 > subq_0.c0) then 1 else 0 end + 0)
FROM (
    SELECT ref_1.id AS c0
    FROM comments AS ref_0
    LEFT JOIN comments AS ref_1
        ON ref_0.id > ref_1.id
    INNER JOIN comments AS ref_2
        ON (
            (SELECT AVG(id) FROM orders) != 10.28
            OR ref_1.is_spam != ref_2.is_spam
        )
    WHERE CASE
            WHEN 64.13 = (SELECT AVG(id) FROM comments) THEN 60.96
            ELSE CASE
                    WHEN ref_0.post_id < ref_2.user_id THEN 61.49
                    ELSE 30.99
                 END
          END >= 21.42
) AS subq_0
----
0

statement ok
DROP TABLE comments

statement ok
DROP TABLE orders

statement ok
SET SCHEMA sys

statement ok
DROP SCHEMA s7900
