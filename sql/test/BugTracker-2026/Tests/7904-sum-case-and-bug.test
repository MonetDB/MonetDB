-- SCHEMA

statement ok
CREATE TABLE comments7904 (
    id          INT,
    post_id     INT,
    user_id     INT,
    content     VARCHAR(1000),
    is_spam     INT,
    created_at  TIMESTAMP
)

statement ok rowcount 4
INSERT INTO comments7904 VALUES
(1, 1, 2, 'Nice post', 0, '2022-01-20 10:00:00'),
(2, 1, 3, 'Spam here', 1,  '2022-01-21 11:00:00'),
(3, 2, 1, 'Thanks',    0, '2022-01-22 12:00:00'),
(4, 4, 5, NULL,        0, '2022-01-23 13:00:00')

-- RESULT: {0}
query I nosort
SELECT COUNT(*)
FROM comments7904 AS ref_0
WHERE (
    LTRIM('dbb') <= '8e'
    AND ref_0.user_id IS NOT NULL
)
----
0

-- RESULT was: {4} wrong!
query I nosort
SELECT SUM(
    CASE
        WHEN (
            LTRIM('dbb') <= '8e'
            AND ref_0.user_id IS NOT NULL
        )
        THEN 1 ELSE 0
    END + 0
)
FROM comments7904 AS ref_0
----
0

statement ok
DROP TABLE comments7904
