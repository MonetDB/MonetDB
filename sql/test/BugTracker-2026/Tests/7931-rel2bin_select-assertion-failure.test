statement ok
CREATE SCHEMA test7931

statement ok
SET SCHEMA test7931

statement ok
CREATE TABLE users (
    id           INT,
    username     VARCHAR(100),
    email        VARCHAR(255),
    age          INT,
    status       VARCHAR(20),
    created_at   TIMESTAMP,
    score        DOUBLE
)

statement ok
CREATE TABLE posts (
    id          INT,
    user_id     INT,
    title       VARCHAR(255),
    content     VARCHAR(1000),
    views       INT,
    likes       INT,
    created_at  TIMESTAMP,
    rating      DOUBLE
)

statement ok
CREATE TABLE orders (
    id          INT,
    user_id     INT,
    amount      DOUBLE,
    status      VARCHAR(20),
    created_at  TIMESTAMP
)

statement ok rowcount 5
INSERT INTO users VALUES
(1, 'alice', 'alice@test.com', 20, 'active',  '2022-01-01 10:00:00', 88.5),
(2, 'bob',   'bob@test.com',   30, 'active',  '2022-01-02 11:00:00', 92.3),
(3, 'carol', NULL,             NULL, 'banned','2022-01-03 12:00:00', NULL),
(4, 'dave',  'dave@test.com',  45, 'active',  '2022-01-04 13:00:00', 65.2),
(5, NULL,    'null@test.com',  18, 'inactive','2022-01-05 14:00:00', 70.0)

statement ok rowcount 5
INSERT INTO posts VALUES
(1, 1, 'Hello World', 'First post', 100, 10, '2022-01-10 10:00:00', 4.5),
(2, 1, 'Another Post', NULL,        150, 20, '2022-01-11 11:00:00', 3.0),
(3, 2, 'Bob Post',     'Content',   NULL,  5, '2022-01-12 12:00:00', NULL),
(4, 3, NULL,           'Empty',     50,   2, '2022-01-13 13:00:00', 5.0),
(5, 4, 'Last Post',    'Last',      300,  30,'2022-01-14 14:00:00', 4.9)

statement ok rowcount 5
INSERT INTO orders VALUES
(1, 1, 100.00, 'paid',    '2022-02-01 09:00:00'),
(2, 1, 200.50, 'shipped', '2022-02-02 10:00:00'),
(3, 2, NULL,   'failed',  '2022-02-03 11:00:00'),
(4, 3, 50.00,  'paid',    '2022-02-04 12:00:00'),
(5, 5, 999.99, 'paid',    '2022-02-05 13:00:00')

-- sql/backends/monet5/rel_bin.c:4988: rel2bin_select: Assertion `sql->session->status == -10' failed.
query I nosort
SELECT 1
FROM users AS ref_0,
     LATERAL (
        SELECT
            ref_0.username AS c0,
            ref_0.email AS c1
        FROM orders AS ref_1
        WHERE ref_1.user_id > ASCII('b2q5')
        LIMIT 31
     ) AS subq_0
WHERE (NULLIF(NULLIF('phlr', 'd'), '3s07')  > '78')
  AND (NULLIF('9', 's0s')
       <= SUBSTRING(
            COALESCE(COALESCE('8i', 'qbaujg'), 'h2'),
            NULLIF(
                CASE
                    WHEN (FALSE) AND (ref_0.id != ref_0.id)
                    THEN '2q8ugv'
                    ELSE CASE
                            WHEN EXISTS (
                                    SELECT 1
                                    FROM users AS ref_3
                                    WHERE ((EXISTS (
                                                SELECT 1
                                                FROM posts AS ref_6,
                                                     LATERAL (
                                                        SELECT 1
                                                        FROM users AS ref_7,
                                                             LATERAL (
                                                                SELECT subq_0.c1 AS c0
                                                                FROM users AS ref_8
                                                                WHERE TRUE
                                                                LIMIT 132
                                                             ) AS subq_3
                                                        WHERE FALSE
                                                        LIMIT 121
                                                     ) AS subq_4
                                                WHERE TRUE
                                            ))
                                        OR (('i' <= 'yl')
                                            AND ((67.11 <> (SELECT VAR_SAMP(id) FROM orders))
                                                AND ('twax' < 'f')))
                                        OR ((subq_0.c0 IS NULL) AND (TRUE)))
                                    LIMIT 127
                                 )
                            THEN 'elge'
                            ELSE 'yg7l'
                         END
                END,
                'o27i8'
            )
         )
      )
----

statement ok
DROP TABLE users

statement ok
DROP TABLE posts

statement ok
DROP TABLE orders

statement ok
SET SCHEMA sys

statement ok
DROP SCHEMA test7931

