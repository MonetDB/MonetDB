statement ok
CREATE SCHEMA test7927

statement ok
SET SCHEMA test7927

statement ok
CREATE TABLE posts (
    id          INT,
    user_id     INT,
    title       VARCHAR(255),
    content     VARCHAR(1000),
    views       INT,
    likes       INT,
    created_at  TIMESTAMP,
    rating      DOUBLE
)

statement ok
CREATE TABLE comments (
    id          INT,
    post_id     INT,
    user_id     INT,
    content     VARCHAR(1000),
    is_spam     INT,
    created_at  TIMESTAMP
)

statement ok
CREATE TABLE orders (
    id          INT,
    user_id     INT,
    amount      DOUBLE,
    status      VARCHAR(20),
    created_at  TIMESTAMP
)

statement ok rowcount 5
INSERT INTO posts VALUES
(1, 1, 'Hello World', 'First post', 100, 10, '2022-01-10 10:00:00', 4.5),
(2, 1, 'Another Post', NULL,        150, 20, '2022-01-11 11:00:00', 3.0),
(3, 2, 'Bob Post',     'Content',   NULL,  5, '2022-01-12 12:00:00', NULL),
(4, 3, NULL,           'Empty',     50,   2, '2022-01-13 13:00:00', 5.0),
(5, 4, 'Last Post',    'Last',      300,  30,'2022-01-14 14:00:00', 4.9)

statement ok rowcount 4
INSERT INTO comments VALUES
(1, 1, 2, 'Nice post', 0, '2022-01-20 10:00:00'),
(2, 1, 3, 'Spam here', 1,  '2022-01-21 11:00:00'),
(3, 2, 1, 'Thanks',    0, '2022-01-22 12:00:00'),
(4, 4, 5, NULL,        0, '2022-01-23 13:00:00')

statement ok rowcount 5
INSERT INTO orders VALUES
(1, 1, 100.00, 'paid',    '2022-02-01 09:00:00'),
(2, 1, 200.50, 'shipped', '2022-02-02 10:00:00'),
(3, 2, NULL,   'failed',  '2022-02-03 11:00:00'),
(4, 3, 50.00,  'paid',    '2022-02-04 12:00:00'),
(5, 5, 999.99, 'paid',    '2022-02-05 13:00:00')

-- trigger sql
query I rowsort
SELECT
    STARTSWITH(
        '77zral',
        CASE
            WHEN EXISTS (
                    SELECT 1
                    FROM comments AS ref_1,
                         LATERAL (
                            SELECT 1
                            FROM posts AS ref_2
                            WHERE (FALSE)
                               OR ((EXISTS (
                                        SELECT 1
                                        FROM comments AS ref_3,
                                             LATERAL (
                                                SELECT
                                                    ref_0.post_id AS c0,
                                                    ref_0.id AS c1,
                                                    ref_0.user_id AS c2
                                                FROM orders AS ref_4
                                                WHERE 81.83 > 51.3
                                             ) AS subq_0
                                        WHERE (3.76 > 13.64)
                                           OR (ref_3.user_id <> ref_1.is_spam)
                                        LIMIT 105
                                    ))
                                   AND (FALSE))
                         ) AS subq_1
                    WHERE 'sv6r' < 'vh8'
                )
            THEN '5'
            ELSE 'iu7dp'
        END
    ) AS c2
FROM comments AS ref_0
WHERE ref_0.id <= ref_0.post_id
LIMIT 79
----
0

statement ok
DROP TABLE posts

statement ok
DROP TABLE comments

statement ok
DROP TABLE orders

statement ok
SET SCHEMA sys

statement ok
DROP SCHEMA test7927

