statement ok
CREATE SCHEMA test7928

statement ok
SET SCHEMA test7928

statement ok
CREATE TABLE users (
    id           INT,
    username     VARCHAR(100),
    email        VARCHAR(255),
    age          INT,
    status       VARCHAR(20),
    created_at   TIMESTAMP,
    score        DOUBLE
)

statement ok
CREATE TABLE posts (
    id          INT,
    user_id     INT,
    title       VARCHAR(255),
    content     VARCHAR(1000),
    views       INT,
    likes       INT,
    created_at  TIMESTAMP,
    rating      DOUBLE
)

statement ok
CREATE TABLE comments (
    id          INT,
    post_id     INT,
    user_id     INT,
    content     VARCHAR(1000),
    is_spam     INT,
    created_at  TIMESTAMP
)

statement ok rowcount 5
INSERT INTO users VALUES
(1, 'alice', 'alice@test.com', 20, 'active',  '2022-01-01 10:00:00', 88.5),
(2, 'bob',   'bob@test.com',   30, 'active',  '2022-01-02 11:00:00', 92.3),
(3, 'carol', NULL,             NULL, 'banned','2022-01-03 12:00:00', NULL),
(4, 'dave',  'dave@test.com',  45, 'active',  '2022-01-04 13:00:00', 65.2),
(5, NULL,    'null@test.com',  18, 'inactive','2022-01-05 14:00:00', 70.0)

statement ok rowcount 5
INSERT INTO posts VALUES
(1, 1, 'Hello World', 'First post', 100, 10, '2022-01-10 10:00:00', 4.5),
(2, 1, 'Another Post', NULL,        150, 20, '2022-01-11 11:00:00', 3.0),
(3, 2, 'Bob Post',     'Content',   NULL,  5, '2022-01-12 12:00:00', NULL),
(4, 3, NULL,           'Empty',     50,   2, '2022-01-13 13:00:00', 5.0),
(5, 4, 'Last Post',    'Last',      300,  30,'2022-01-14 14:00:00', 4.9)

statement ok rowcount 4
INSERT INTO comments VALUES
(1, 1, 2, 'Nice post', 0, '2022-01-20 10:00:00'),
(2, 1, 3, 'Spam here', 1,  '2022-01-21 11:00:00'),
(3, 2, 1, 'Thanks',    0, '2022-01-22 12:00:00'),
(4, 4, 5, NULL,        0, '2022-01-23 13:00:00')

-- sql/backends/monet5/rel_bin.c:3969: rel2bin_semijoin: Assertion `sql->session->status == -10' failed.
query I nosort
SELECT 1
FROM comments AS ref_0
WHERE (
    ''
    <=
    CASE
        WHEN ref_0.is_spam < ref_0.is_spam
        THEN CASE
                WHEN ((ref_0.post_id IS NULL)
                      OR EXISTS (
                            SELECT DISTINCT ref_0.id AS c0
                            FROM users AS ref_1
                            WHERE FALSE AND TRUE
                         ))
                THEN 'y'
                ELSE '16wm0n'
             END
        ELSE 'fl8w'
    END
)
OR (
    COALESCE(
        NULLIF(
            '8n1',
            RTRIM(
                CASE
                    WHEN EXISTS (
                            SELECT 1
                            FROM users AS ref_2,
                                 LATERAL (
                                    SELECT
                                        ref_0.user_id AS c0,
                                        ref_0.post_id AS c1,
                                        ref_2.id AS c2
                                    FROM posts AS ref_3
                                    WHERE ref_3.created_at IS NOT NULL
                                    LIMIT 37
                                 ) AS subq_0
                            WHERE ref_0.user_id > ref_0.post_id
                         )
                    THEN 'k'
                    ELSE 'xhbq'
                END
            )
        ),
        ''
    ) <> ''
)
----
1
1
1
1

statement ok
DROP TABLE users

statement ok
DROP TABLE posts

statement ok
DROP TABLE comments

statement ok
SET SCHEMA sys

statement ok
DROP SCHEMA test7928

