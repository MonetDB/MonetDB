query I rowsort
SELECT (COUNT(*) > 0) AS has_rows FROM sys.spatial_ref_sys
----
1

statement error
TRUNCATE TABLE sys.spatial_ref_sys

query I rowsort
SELECT (COUNT(*) > 0) AS has_rows FROM sys.spatial_ref_sys
----
1

