statement ok
set optimizer='default_pipe'

statement ok
set optimizer='minimal_pipe'

query T rowsort
select optimizer
----
minimal_pipe

statement error
set optimizer=' optimizer.inline(); optimizer.remap(); optimizer.evaluate(); optimizer.costModel(); optimizer.coercions(); optimizer.emptySet(); optimizer.aliases(); optimizer.mergetable(); optimizer.deadcode(); optimizer.commonTerms(); optimizer.joinPath(); optimizer.reorder(); optimizer.deadcode(); optimizer.reduce(); optimizer.querylog(); optimizer.multiplex(); optimizer.garbageCollector();'

query T rowsort
select optimizer
----
minimal_pipe

statement error
set optimizer='myfamous_pipe'

query T rowsort
select optimizer
----
minimal_pipe

query TTT nosort
select * from optimizers()
----
minimal_pipe
optimizer.minimalpipe();
stable
default_pipe
optimizer.defaultpipe();
stable
no_mitosis_pipe
optimizer.nomitosispipe();
stable
sequential_pipe
optimizer.sequentialpipe();
stable
recursive_pipe
optimizer.recursivepipe();
stable

statement ok
set optimizer='default_pipe'

