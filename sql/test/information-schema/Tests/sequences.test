query TTTTIIIIIIITTII rowsort
SELECT
  SEQUENCE_CATALOG,
  SEQUENCE_SCHEMA,
  SEQUENCE_NAME,
  DATA_TYPE,
  NUMERIC_PRECISION,
  NUMERIC_PRECISION_RADIX,
  NUMERIC_SCALE,
  START_VALUE,
  MINIMUM_VALUE,
  MAXIMUM_VALUE,
  INCREMENT,
  CYCLE_OPTION,
  DECLARED_DATA_TYPE,
  DECLARED_NUMERIC_PRECISION,
  DECLARED_NUMERIC_SCALE
FROM INFORMATION_SCHEMA.SEQUENCES
WHERE SEQUENCE_SCHEMA = '' OR SEQUENCE_NAME = ''
----

query TTTTIIIIIIITTIIIIIIT rowsort
SELECT
  SEQUENCE_CATALOG,
  SEQUENCE_SCHEMA,
  SEQUENCE_NAME,
  DATA_TYPE,
  NUMERIC_PRECISION,
  NUMERIC_PRECISION_RADIX,
  NUMERIC_SCALE,
  START_VALUE,
  MINIMUM_VALUE,
  MAXIMUM_VALUE,
  INCREMENT,
  CYCLE_OPTION,
  DECLARED_DATA_TYPE,
  DECLARED_NUMERIC_PRECISION,
  DECLARED_NUMERIC_SCALE,
  schema_id,
  sequence_id,
  current_value,
  cacheinc,
  comments
FROM INFORMATION_SCHEMA.SEQUENCES
WHERE SEQUENCE_SCHEMA = '' OR SEQUENCE_NAME = ''
----

query ITTT rowsort
SELECT COUNT(*) AS duplicates, SEQUENCE_CATALOG, SEQUENCE_SCHEMA, SEQUENCE_NAME
 FROM INFORMATION_SCHEMA.SEQUENCES
 GROUP BY SEQUENCE_CATALOG, SEQUENCE_SCHEMA, SEQUENCE_NAME
 HAVING COUNT(*) > 1
----

