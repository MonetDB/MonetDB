-- test with 'monetdb://localhost:$MAPIPORT/$TSTDB/sys/table;' protocol specification

query I
select count(*) from 'monetdb://localhost:$MAPIPORT/$TSTDB/sys/_tables'
----
143

statement error 42000!SELECT: proto_loader function failed 'Failed to access remote server'
select * from 'monetdb://localhost:$MAPIPORT/$TSTDB/sys/tables2'
