MONET SQL FRONTEND

Installation
------------

Install Monet, then do the usual ./bootstrap, mkdir Linux, cd Linux, ../configure,
make, make install sequence. 

Edit your monet.conf to include the sql-libraries in the monet_mod_path:
  monet_mod_path=${exec_prefix}/lib/MonetDB:${exec_prefix}/lib:${exec_prefix}/lib/sql


Usage
-----

Start Mserver, and do:
  const MONET_PREFIX := environment.find("exec_prefix");

  source ( MONET_PREFIX + "/share/sql/catalog.mil");
  source ( MONET_PREFIX + "/share/sql/sqlserver.mil" );

then use sql_client for issueing SQL commands.

