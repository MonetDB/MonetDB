## Process this file with automake to produce Makefile.in

EXTRA_DIST = sqlserver.mil

INCLUDES = $(MONET_INCS) ../common ../client 

lib__sqlserver = {
	DIR = pkglibdir
	SOURCES = sqlserver.mx mvc.mx logger.mx privileges.mx \
		catalog_module.c statement_exec.c 
	LIBS = ../common/libsql $(MONET_LIBS)
}

scripts_mil = {
	DIR = pkgdatadir
	SOURCES = sqlserver.mil
}
