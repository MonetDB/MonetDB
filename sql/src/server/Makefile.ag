## Process this file with automake to produce Makefile.in

EXTRA_DIST = catalog.mil sqlserver.mil

INCLUDES = \
	-I$(MONET_PREFIX)/include \
	-I$(MONET_PREFIX)/include/gdk \
	-I$(MONET_PREFIX)/include/monet \
	-I$(MONET_PREFIX)/include/plain \
	-I$(MONET_PREFIX)/include/stream \
	-I${top_srcdir}/src/common \
	-I${top_builddir}/src/common \
	-I${top_srcdir}/src/client \
	$(GETOPT_INCS)

lib__sqlserver = {
	SOURCES = sqlserver.mx mvc.mx logger.mx \
		catalog_module.c statement_exec.c
	LIBS = ../common/libsql \
		-L$(MONET_PREFIX)/lib/Monet 
}

scripts_mil = {
        DIR = pkgdatadir
        SOURCES = catalog.mil sqlserver.mil
}
