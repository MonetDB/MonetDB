## Process this file with automake to produce Makefile.in

BUILT_SOURCES = cSQLserver.c

INCLUDES = \
	-I$(MONET_PREFIX)/include \
	-I$(MONET_PREFIX)/include/gdk \
	-I$(MONET_PREFIX)/include/monet \
	-I$(MONET_PREFIX)/include/plain \
	-I$(MONET_PREFIX)/include/stream \
	-I${top_srcdir}/src/common \
	$(GETOPT_INCS)

lib__sqlserver = {
	SOURCES = sqlserver.mx 
	LIBS = -L$(MONET_PREFIX)/lib/Monet
}

lib__mvc = {
	SOURCES = mvc.mx cCatalog.c cCatalog.h
	LIBS = -L$(MONET_PREFIX)/lib/Monet
}

scripts_mil = {
        DIR = pkgdatadir
        SOURCES = catalog.mil sqlserver.mil
}
