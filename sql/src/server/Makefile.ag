## Process this file with automake to produce Makefile.in

INCLUDES = ../common $(MONET_INCS)

YFLAGS = -d -p sql

lib_sqlserver = {
	DIR = libdir
	SOURCES = \
		sql_atom.mx \
		sql_datetime.mx \
		sql_string.mx \
		sql_decimal.mx \
		sql_scope.mx \
		sql_types.mx \
		sql_statement.mx \
		sql_semantic.mx \
		sql_optimize.mx \ 
		sql_rel2bin.mx \
		sql_qc.mx \
		sql_sym.mx \
		sql_symbol.mx \
		sql_scan.mx \   
		sql_parser.mx \  
		sql_bm.mx \ 
		sql_logger.mx \      
		sql_mvc.mx \    
		sql_privileges.mx \  
		sql_context.mx \
		sql_gencode.mx
	WINLIBS = -lbat -lmonet -lstream -l_streams -l_kernel -l_ascii_io -l_algebra
	LIBS = ../common/libsql $(MONET_LIBS)
}

