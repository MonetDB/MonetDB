See Makefile for how to build.

Try this for testing:

import odbc
env = odbc.SQLAllocEnv()[1]
dbc = odbc.SQLAllocConnect(env)[1]
odbc.SQLConnect(dbc, 'localhost', 'monetdb', 'monetdb')
stmt = odbc.SQLAllocStmt(dbc)[1]

or alternatively:

import odbc
env = odbc.SQLAllocHandle(odbc.SQL_HANDLE_ENV, None)[1]
dbc = odbc.SQLAllocHandle(odbc.SQL_HANDLE_DBC, env)[1]
odbc.SQLConnect(dbc, 'localhost', 'monetdb', 'monetdb')
stmt = odbc.SQLAllocHandle(odbc.SQL_HANDLE_STMT, dbc)[1]

import sys
sys.path.insert(0,"build/lib.linux-i686-2.4")
import odbc
env = odbc.SQLAllocHandle(odbc.SQL_HANDLE_ENV, None)[1]
dbc = odbc.SQLAllocHandle(odbc.SQL_HANDLE_DBC, env)[1]
odbc.SQLConnect(dbc, "localhost", "monetdb", "monetdb")
stmt = odbc.SQLAllocHandle(odbc.SQL_HANDLE_STMT, dbc)[1]
odbc.SQLExecDirect(stmt, "CREATE DATABASE absentee")
odbc.SQLFreeHandle(odbc.SQL_HANDLE_STMT, stmt)
stmt = odbc.SQLAllocHandle(odbc.SQL_HANDLE_STMT, dbc)[1]
odbc.SQLExecDirect(stmt, "USE absentee")
odbc.SQLFreeHandle(odbc.SQL_HANDLE_STMT, stmt)
stmt = odbc.SQLAllocHandle(odbc.SQL_HANDLE_STMT, dbc)[1]
odbc.SQLExecDirect(stmt, """CREATE TABLE absent (
    person SMALLINT UNSIGNED,
    start DATE,
    end DATE,
    period ENUM('Morning','Afternoon','Whole day'),
    frequency TINYINT UNSIGNED,
    reason VARCHAR(80),
    id SMALLINT UNSIGNED NOT NULL AUTO_INCREMENT,
    PRIMARY KEY (id),
    gid VARCHAR(5) DEFAULT '')""")
odbc.SQLFreeHandle(odbc.SQL_HANDLE_STMT, stmt)
stmt = odbc.SQLAllocHandle(odbc.SQL_HANDLE_STMT, dbc)[1]
odbc.SQLExecDirect(stmt, "GRANT SELECT,INSERT,UPDATE,DELETE ON absentee.absent TO monetdb@localhost")
odbc.SQLFreeHandle(odbc.SQL_HANDLE_STMT, stmt)
stmt = odbc.SQLAllocHandle(odbc.SQL_HANDLE_STMT, dbc)[1]
odbc.SQLExecDirect(stmt, "INSERT INTO absent SET person = 234, start = '2003-9-14', end = '2003-9-22', period = 'Whole day', frequency = 1, reason = 'Vacation', gid = ''")
odbc.SQLFreeHandle(odbc.SQL_HANDLE_STMT, stmt)
