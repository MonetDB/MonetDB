#!/bin/sh
PWD=`pwd`
SQL=sql_client
if [ $# -eq 1 ]; then
	SQL=$1
fi

echo "creating"
$SQL < as3ap-schema.sql
echo "loading"
cat as3ap-load.sql | sed -e s+PWD+$PWD+ | $SQL 
#echo "queries"
#$SQL < as3ap-queries.sql
