#!/bin/sh
PWD=`pwd`
SQL="MapiClient -lsql -u monetdb -P monetdb --debug=25"
if [ $# -eq 1 ]; then
	SQL=$1
fi

echo "creating"
$SQL < as3ap-schema.sql
echo "loading"
cat as3ap-load.sql | sed -e s+PWD+$PWD+ | $SQL 
echo "queries"
$SQL < as3ap-queries.sql
echo "drop"
$SQL < drop.sql
