#!/bin/sh
PWD=`pwd`
echo $PWD
SQL=sql_client
if [ $# -eq 1 ]; then
	SQL=$1
fi

#Generate Data...

#gcc -o earth earth.c
#./earth

#gcc -o tomo  tomo.c
#./tomo

#Bulk-Load into SQL server...

$SQL < create.sql
cat load.sql | sed -e s+PWD+$PWD+ | $SQL 

$SQL < query_1.sql
$SQL < query_2.sql
$SQL < query_3.sql

#Cleanup

#rm earth.tab earth
#rm tomo.tab tomo

