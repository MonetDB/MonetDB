#!/usr/bin/env bash

# create
MapiClient -lsql -u monetdb -P monetdb --port=$SQLPORT < create_tables.flt.sql

# check0
MapiClient -lsql -u monetdb -P monetdb --port=$SQLPORT < Tests/check0.sql

# insert_MODEL
MapiClient -lsql -u monetdb -P monetdb --port=$SQLPORT < insert_MODEL.flt.sql

# insert_ATOM
MapiClient -lsql -u monetdb -P monetdb --port=$SQLPORT < insert_ATOM.flt.sql

# insert_BOND
MapiClient -lsql -u monetdb -P monetdb --port=$SQLPORT < insert_BOND.flt.sql

# check1
MapiClient -lsql -u monetdb -P monetdb --port=$SQLPORT < Tests/check0.sql

