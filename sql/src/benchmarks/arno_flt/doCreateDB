#!/usr/bin/env bash

# create
sql_client --port=$SQLPORT < create_tables.flt.sql

# check0
sql_client --port=$SQLPORT < Tests/check0.sql

# insert_MODEL
sql_client --port=$SQLPORT < insert_MODEL.flt.sql

# insert_ATOM
sql_client --port=$SQLPORT < insert_ATOM.flt.sql

# insert_BOND
sql_client --port=$SQLPORT < insert_BOND.flt.sql

# check1
sql_client --port=$SQLPORT < Tests/check0.sql

