#!/usr/bin/env bash

# create
Msql -u monetdb -P monetdb --port=$SQLPORT < create_tables.flt.sql

# check0
Msql -u monetdb -P monetdb --port=$SQLPORT < Tests/check0.sql

# insert_MODEL
Msql -u monetdb -P monetdb --port=$SQLPORT < insert_MODEL.flt.sql

# insert_ATOM
Msql -u monetdb -P monetdb --port=$SQLPORT < insert_ATOM.flt.sql

# insert_BOND
Msql -u monetdb -P monetdb --port=$SQLPORT < insert_BOND.flt.sql

# check1
Msql -u monetdb -P monetdb --port=$SQLPORT < Tests/check0.sql

