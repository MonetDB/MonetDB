#!/bin/sh
PWD=`pwd`
SQL="MapiClient -lsql -u monetdb -P monetdb"
if [ $# -eq 1 ]; then
	SQL=$1
fi

echo "creating"
$SQL < create.sql
echo "loading"
cat load.sql | sed -e s+PWD+$PWD+ | $SQL 
echo "queries"
$SQL < query.sql
