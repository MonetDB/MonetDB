#!/bin/sh
PWD=`pwd`
echo $PWD
SQL='Msql -u monetdb -P monetdb'
if [ $# -eq 1 ]; then
	SQL=$1
fi

#Generate Data...

#Bulk-Load into SQL server...

$SQL < VOCschema.sql
$SQL < VOCinsert.sql
cat load.sql | sed -e s+PWD+$PWD+ | $SQL 

$SQL < VOCquery.sql

#Cleanup
