var mods := new(void,str);
mods.insert(nil,"sql_server2");

loaded;
mods@batloop{
	printf("loading module '%s'\n",$t);
	module($t);
	loaded;
	printf("dropping module '%s'\n",$t);
	drop($t);
	loaded;
}

quit;
