
INCLUDES = ../../common ../../server $(MONET5_INCS)

lib__sql = {
	DIR = libdir/MonetDB5
	SOURCES = \
		sql.mx \
		sql_scenario.mx \
		sql_gencode.mx \
		sql_server.mx
	WINLIBS = -lbat -lmonet -lstream -l_streams -l_bat -l_kernel -l_ascii_io
	LIBS = ../../server/libsqlserver $(MONET_LIBS) -lbat -lmal -lstream $(MONET5_LIBS)
}

scripts_mal = {
	DIR = libdir/MonetDB5
	SOURCES = sql.mx sql_scenario.mx sql_server.mx
}
