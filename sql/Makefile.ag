
SUBDIRS = conf src 
EXTRA_DIST = rules.mk RunMtest.in RunMtest.bat.in \
		MonetDB-SQL.spec.in MonetDB-SQL.spec mknife.gif \
		SQLfeatures.tex HowToStart-SQL HowToStart-SQL-Win32.txt
EXTRA_DIST_DIR = NT

EXTRA_HEADERS = sql_config.h

headers_pc = {
	HEADERS = pc
	SOURCES	= MonetDB-SQL.pc.in
	DIR = libdir/pkgconfig
}

doc_sql = {
	DIR = datadir/MonetDB/docs/SQL
	SOURCES = SQLfeatures.tex mknife.gif
}

TESTS = RunMtest
TESTS_ENVIRONMENT = $(SHELL)
