
# Second client does load module "lock" him/herself.
# Works as expected.

module(lock);

# should show module "lock" with refcount 1:
loaded();

# should all succeed:
sigs("lock");
bat("test").print();
bat("test").count().print();
