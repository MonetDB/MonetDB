stdout of test 'mprof_niels` in directory 'src/modules/contrib` itself:

#> Mtest:	other mprof_niels
#> Mtest:	./mprof_niels mprof_niels
#> Mlog:	Mserver -yaccsilent -d8 -home /ufs/manegold/Current/monet/IRIX6.3 -db modules_plain -single /ufs/manegold/Current/monet/IRIX6.3/modules/plain/niels.mil
# Monet Database Server V4.0
# Copyright (c) 1993-1998, CWI & SION. All rights reserved.
# compiled for IRIX6.3/32bit on Thu Sep 17 19:13:21 MET DST 1998.
#script:/ufs/manegold/Current/monet/IRIX6.3/modules/plain/niels.mil


#You are system administrator (adm)!

#line 1399 "modules/plain/mprof.mx"
setoid(oid(20000000));
module(mprof);
!ERROR: locate module: bulk.
!ERROR: TBL_loadmod: module load bulk cancelled.
module(gdkmem);module(bulk);
!ERROR: CMDmodule: load failed.
!ERROR: locate module: integer.
!ERROR: TBL_loadmod: module load integer cancelled.
module(integer,groupFunctional);
!ERROR: CMDmodule: load failed.


proc pmSummary := {
	mprof_cnt := mprof_interval.head(1).sort;
!ERROR: "	mprof_cnt := mprof_interval.head(1).sort;"
!        	                                         ^
! can't help you here, sorry.

proc pmPrint := {
   	mprof_cpu := mprof_Ecpu [-] mprof_Bcpu;
   	mprof_io  := mprof_Eio  [-] mprof_Bio;
   	mprof_mem := mprof_Emem [-] mprof_Bmem;

	mprof_cpu := mprof_interval.join(mprof_cpu).sort;
	mprof_io  := mprof_interval.join(mprof_io ).sort;
	mprof_mem := mprof_interval.join(mprof_mem).sort;

!ERROR: "	mprof_mem := mprof_interval.join(mprof_mem).sort;"
!        	                                                 ^
! can't help you here, sorry.

quit();
