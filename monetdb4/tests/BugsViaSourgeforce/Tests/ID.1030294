#!/bin/sh

Mlog -x	rm -f /tmp/Mserver.init
Mlog -2	"echo 'printf(\"\\\n ... loading my secret module that allows me to open a root shell from MIL ... \\\n\\\n\");' \> /tmp/Mserver.init"
eval	"echo 'printf(\"  \n ... loading my secret module that allows me to open a root shell from MIL ...   \n  \n\");'  > /tmp/Mserver.init"
Mlog -x	chmod a-w,a+r /tmp/Mserver.init
Mlog -2	"echo 'quit();' \| $MSERVER --dbname=$TSTDB --dbinit='printf(\"\\\nHello!\\\n\\\n\");'"
eval	"echo 'quit();'  | $MSERVER --dbname=$TSTDB --dbinit='printf(\"  \nHello!  \n  \n\");'"
Mlog -x	rm -f /tmp/Mserver.init

