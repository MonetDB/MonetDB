
INCLUDES = top_builddir/src/gdk top_builddir/src/monet top_builddir/src/mal

SUBDIRS = clients

MTSAFE

# install these libs in the pkglib
LIBDIR = pkglib

lib__mapi = {
	SOURCES = mapi.mx
	HEADERS = h proto.h m
	SCRIPTS = mil ""
	LIBS = ../gdk/libbat ../monet/libmonet \
		$(SOCKET_LIBS) $(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
}

scripts_mil = {
	DIR = pkgdatadir
	SOURCES = mapi.mil
}

EXTRA_DIST_DIR = Tests
