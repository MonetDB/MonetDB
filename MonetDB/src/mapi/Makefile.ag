
INCLUDES = -I$(MONET_BUILD)/src/gdk -I$(MONET_BUILD)/src/monet \
	   -I$(MONET_BUILD)/src/modules/plain

SUBDIRS = clients

MTSAFE

# install these libs in the pkglib
LIBDIR = pkglib

lib__mapi = {
	SOURCES = mapi.mx
	HEADERS = h proto.h m
	SCRIPTS = mil ""
	LIBS = ../gdk/libbat ../monet/libmonet \
		../modules/plain/lib_kernel \
		$(SOCKET_LIBS) $(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
}

scripts_mil = {
	DIR = pkgdatadir
	SOURCES = mapi.mil
}

EXTRA_DIST_DIR = Tests
