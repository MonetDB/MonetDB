#!/bin/sh

Mlog -x "ant -emacs -buildfile ${TSTSRCDIR}/../build.xml -Dbuilddir=${TSTBLDDIR}/../ -Dhost=\"${HOST}\" -Dport=${MAPIPORT} -Dmapi_jar=${TSTBLDDIR}/../../../clients/java/mapi.jar -Djardir="" ${TST}"
