The source code examples should be compiled with a proper
reference to the MonetDB Java interface library.
The following commands would be necessary if you run
them from this location.

> javac -sourcepath ../../clients/java sample0.java
> java -cp $CLASSPATH:../../clients/java sample0 45123

The argument for sample0 is the default portnumber
hardwired in the SQL support module.

The $CLASSPATH should refer to the commonly used class
libraries, such as....
