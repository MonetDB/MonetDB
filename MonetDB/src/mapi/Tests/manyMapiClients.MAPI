#!/usr/bin/env bash

n=195
Mlog "${n}x MapiClient -everything"
i=0;
while [ $i -lt $n ] ; do
	i=$[i+1]
	echo "$i:"
	echo "$i:" >&2
	$MAPI_CLIENT <<- EOF	# 195x
		print($i);
	EOF
done
echo "done: $i"

