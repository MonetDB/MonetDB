#!/bin/bash 

n=195
#Mlog "${n}x MapiClient -everything"
i=0;
while [ $i -lt $n ] ; do
	i=$[i+1]
	echo "$i:"
	echo "$i:" >&2
#	$MAPICLIENT <<- EOF
	/home/niels/data/monet/Linux/bin/MapiClient -everything <<- EOF
		print($i);
	EOF
done
echo "done: $i"
