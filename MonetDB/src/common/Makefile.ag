## Process this file with automake to produce Makefile.in

MTSAFE

EXTRA_DIST = getopt.h

lib_mutils = { 
	NAME = mutils
	SOURCES = monet_utils.mx monet_options.mx.in @LIBOBJS@
	HEADERS = h
	LIBS = @LIBOBJS@
}
