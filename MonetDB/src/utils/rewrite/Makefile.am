## Process this file with automake to produce Makefile.in

#AUTOMAKE_OPTIONS = no-dependencies

BUILT_SOURCES = 

EXTRA_DIST = 

#-version-info $(LT_VERSION)
bin_PROGRAMS = rewrite

rewrite_SOURCES = \
	rewrite.c
