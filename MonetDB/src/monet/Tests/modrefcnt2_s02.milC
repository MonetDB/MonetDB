
# Third client again does not load module "lock" him/herself.
# Same situation as with first client.

# should not show module "lock":
loaded();
sigs("lock");
bat("test").print();
bat("test").count().print();

# Consistency check:
# Trying to drop module "lock" here should give an error.
drop("lock");

# should not show module "lock":
loaded();

# should fail:
sigs("lock");

