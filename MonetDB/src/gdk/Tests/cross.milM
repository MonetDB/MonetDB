var a:=new(void,int).seqbase(0@0).reverse;
a.print;
var b:=new(void,int).seqbase(0@0);
b.print;

cross(a,a).print;
cross(b,b).print;
cross(a,b).print;
cross(b,a).print;

a.insert(1,nil);
a.print;
cross(a,a).print;
cross(b,b).print;
cross(a,b).print;
cross(b,a).print;

b.insert(nil,11);
b.print;
cross(a,a).print;
cross(b,b).print;
cross(a,b).print;
cross(b,a).print;

a.insert(2,nil); 
a.print;
cross(a,a).print;
cross(b,b).print;
cross(a,b).print;
cross(b,a).print;

b.insert(nil,22);
b.print;
cross(a,a).print;
cross(b,b).print;
cross(a,b).print;
cross(b,a).print;
