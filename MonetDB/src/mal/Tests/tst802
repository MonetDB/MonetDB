# using spaces

u:= bind(str,"user");
print("User bound to %s\n",u);

v:= mil.bind("roomnumber");
v:= 123;
v:= v+1;
mil.release("roomnumber",v);
mil.release("roomnumber",v); # failure? done twice or implicit bind?
