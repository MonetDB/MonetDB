# check propagation of type variables over bats
# can be checked after bat module has been added

module bat;
command new(any::1 h,any::2 t):bat[any::1,any::2]= GDKbatnew;

a:= bat.new(str,int);
b:= bat.new(str,int);
c:= new(str,int);
