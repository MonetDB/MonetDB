@f drop
@a Niels Nes

@h
#ifndef _DROP_H_
#define _DROP_H_

#include "symbol.h"

class Drop : public Symbol {
    public:
	Drop( int t, char *n ); 

	virtual ostream &print( language *l, ostream &o ) const ;
};
#endif // _DROP_H_

@C
#include "drop.h"
#include "language.h"
#include <string.h>


Drop::Drop( int t, char *n ) : Symbol(t,n) { }

ostream &Drop::print( language *l, ostream &o ) const {
	return l->gen_drop( o, *this );
}
