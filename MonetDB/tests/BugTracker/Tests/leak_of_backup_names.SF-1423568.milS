
var vm := lng(0);
PROC pro() : bat[void,int] {
	new(void,int);
	if (vm = lng(0)) {
		vm := vm_cursize();
	} else {
		var cur := vm_cursize();
		if (vm < cur) {
			printf("grew %lld bytes\n", cur - vm);
			vm := cur;
		}
	}
}

var i := 0;
while (i<100) {
	pro();
	i:+=1;
}
