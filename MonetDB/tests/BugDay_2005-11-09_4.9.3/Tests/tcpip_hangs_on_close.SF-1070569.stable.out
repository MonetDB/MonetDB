stdout of test 'tcpip_hangs_on_close.SF-1070569` in directory 'tests/BugDay_2005-11-09_4.9.3` itself:


# 12:33:36 >  
# 12:33:36 >  Mtimeout -timeout 60 Mserver "--config=/ufs/alink/local/etc/MonetDB.conf" --debug=10 --set "monet_mod_path=/ufs/alink/local/lib/MonetDB:/ufs/alink/local/lib/bin" --set "gdk_dbfarm=/ufs/alink/local/var/MonetDB/dbfarm" --set "sql_logdir=/ufs/alink/local/var/MonetDB/log" --set mapi_port=36985 --set sql_port=43297 --set xquery_port=54389 --set monet_prompt= --trace --dbname=mTests_tests_BugDay_2005-11-09_4.9.3  < tcpip_hangs_on_close.SF-1070569.milS
# 12:33:36 >  

# Monet Database Server V4.9.3
# Copyright (c) 1993-2005, CWI. All rights reserved.
# Compiled for x86_64-redhat-linux-gnu/64bit with 32bit OIDs; dynamically linked.
# Visit http://monetdb.cwi.nl/ for further information.

module(tcpip);
module(alarm);
VAR mapiport := monet_environment.find("mapi_port");

{
fork(listen(int(mapiport)+0));
var y := import("x", true);
y.print();
}
#-----------------#
# h	t	  # name
# oid	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]

{
fork(listen(int(mapiport)+1));
var y := import("x", true);
y.print();
}
#-----------------#
# h	t	  # name
# oid	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]

{
fork(listen(int(mapiport)+2));
var y := import("x", true);
y.print();
}
#-----------------#
# h	t	  # name
# oid	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]

{
fork(listen(int(mapiport)+3));
var y := import("x", true);
y.print();
}
#-----------------#
# h	t	  # name
# oid	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]
quit();
# Monet Database Server V4.11.3
# Copyright (c) 1993-2006, CWI. All rights reserved.
# Compiled for x86_64-redhat-linux-gnu/64bit with 64bit OIDs; dynamically linked.
# Visit http://monetdb.cwi.nl/ for further information.

module(tcpip);
module(alarm);
VAR mapiport := monet_environment.find("mapi_port");

{
sleep(2); # waiting for first server to start listening
VAR c := open("localhost:"+(int(mapiport)+0));
var x := bat(oid, oid);
x.insert(0@0, 0@0);
x.insert(1@0, 0@0);
x.sort().print();
c.export(x, "x");
close(c);
}
#-----------------#
# h	t	  # name
# void	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]

{
sleep(2);	# waiting for first server to start listening
VAR c := open("localhost:"+(int(mapiport)+1));
var x := bat(oid, oid);
x.insert(0@0, 0@0);
x.insert(1@0, 0@0);
x.sort().print();
c.export(x, "x");
close(c);
}
#-----------------#
# h	t	  # name
# void	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]

{
sleep(2);	# waiting for first server to start listening
VAR c := open("localhost:"+(int(mapiport)+2));
var x := bat(oid, oid);
x.insert(0@0, 0@0);
x.insert(1@0, 0@0);
x.sort().print();
c.export(x, "x");
close(c);
}
#-----------------#
# h	t	  # name
# void	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]

{
sleep(2);	# waiting for first server to start listening
VAR c := open("localhost:"+(int(mapiport)+3));
var x := bat(oid, oid);
x.insert(0@0, 0@0);
x.insert(1@0, 0@0);
x.sort().print();
c.export(x, "x");
close(c);
}
#-----------------#
# h	t	  # name
# void	oid	  # type
#-----------------#
[ 0@0,	  0@0	  ]
[ 1@0,	  0@0	  ]
quit();

# 12:33:38 >  
# 12:33:38 >  Done.
# 12:33:38 >  

