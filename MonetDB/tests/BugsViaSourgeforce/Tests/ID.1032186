#!/bin/sh

NAME="$1"

Mlog   "$MSERVER --dbname=$TSTDB $NAME.milS"
eval    $MSERVER --dbname=$TSTDB $NAME.milS

Mlog   "$MSERVER --dbname=$TSTDB < $NAME.milS"
eval    $MSERVER --dbname=$TSTDB < $NAME.milS

