# by Martin Kersten:
# The following one-liner script when ran against
# MapiClient (and from Mcockpit) caused the server to
# hang on an internal Flex error. Perhaps related to the
# recent changes made by Peter in the parser?

ls("address_name"); 
VAR name := nil; IF (view_bbp_name().reverse().exist("address_name")) { name := bat("address_name"); } ELSE { name := new(oid,str); name.rename("address_name"); name.persists(true); }
ls("address_name"); name.print();
\q
