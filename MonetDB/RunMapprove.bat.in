@echo off

if not "%1"=="" goto skip
call %0 -r
goto end

:skip

set builddir=@MONET_BUILD@
set srcdir=@MONET_SOURCE@

set PATH=%builddir%\src\testing;%builddir%\src\tools;%builddir%\src\mapi\clients;%PATH%
set PYTHONPATH=%builddir%\src\testing;%srcdir%\src\testing;%builddir%\src\common;%srcdir%\src\mapi\clients
set PATH=%builddir%\src\common;%builddir%\src\gdk;%builddir%\src\mapi\clients;%builddir%\src\modules\plain\stream;%builddir%\src\monet;%PATH%

REM execute Mapprove.py in the source directory
cd %srcdir%

call python "%builddir%\src\testing\Mapprove.py" "--config=%builddir%\conf\monet.conf" "--TSTTRGBASE=%builddir%" %*

:end
