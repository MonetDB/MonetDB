
These are the new native MonetDB python drivers. These drivers don't depend on
any libraries, so you can run them out of the box. This driver is 100% python
DBAPI complete.

run:

# python setup.py install

to install this module. Check out the examples folder for an example how to use
this module.

Currently only 2.6 is supported, 3.0 is a work in progress. 2.5 support will be
added if possible.


