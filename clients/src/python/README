
This is the native MonetDB python driver. These drivers don't depend on
any libraries, so you can run them out of the box on any platform that supports
python. This driver is 100% python DBAPI 2.0 complete.

run:

# python setup.py install

to install this module. Check out the examples folder for an example how to use
this module.

Only python 2.6 and python 3.0 are supported.

