[PHP]
safe_mode = Off
safe_mode_gid = Off
include_path = ".:@XPHP_PEARDIR@:/usr/share/pear"
extension_dir = @XPHP_EXTENSIONDIR@

;;;;
; Note: packaged extension modules are now loaded via the .ini files
; found in the directory /etc/php.d; these are loaded by default.
;
; This means you need to link the modules in the default extension dir into
; your new extention dir
;;;;
