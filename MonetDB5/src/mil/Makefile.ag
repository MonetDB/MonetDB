INCLUDES = ../common ../gdk ../mal

YFLAGS = -v -d

bin_M2m = {
	SOURCES = M2m.mx  mil_parser.mx mil_conformance.mx \
		mil_commands.mx mil.mx
	LIBS = ../gdk/libbat ../mal/libmal \
		../common/libmutils ../common/libstream  \
                $(SOCKET_LIBS) $(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
}
