# The series tst2?? is a patched version of suite_00
# The first and foremost BAT operations

# String iterator stuff
c:= bat.new(:int,:str);
bat.insert(c,1,"hello");
bat.insert(c,2,"brave");
bat.insert(c,3,"world");
bat.insert(c,4,"");
x:= calc.str(nil);
bat.insert(c,5,x);
bat.insert(c,6,"!");
io.print(c);
barrier (v,h,ts):= chopper.newIterator(c);
	io.printf("[%d,",h);
	io.printf("%s]\n",ts);
	redo (v,h,ts):= chopper.hasMoreElements(c);
exit v;
