barrier (v1,i):= mal.newRange(0);
	io.print("do something");
	redo (v1,i):= mal.nextElement(1,2);
exit	(v1,i);
	io.printf("left first loop with %s\n",v1);
	io.print("next iteration");
barrier (v2,i):= mal.newRange(0);
	io.print("do something else");
	redo (v2,i):= mal.nextElement(2,5);
exit	(v2,i);
