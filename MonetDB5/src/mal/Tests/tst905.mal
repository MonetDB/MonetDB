# the multiplex code blocks.

b:= bat.new(:lng,:lng);
barrier (v,i):= mal.newRange(0:lng);
	bat.insert(b,i,i);
	redo (v,i):= mal.nextElement(1:lng,10:lng);
exit (v,i);
c:= algebra.copy(b);
x:bat[:lng,:lng]:= optimizer.multiplex("calc.+",b,c);
io.print(x);
