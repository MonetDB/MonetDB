# the multiplex code blocks.

b:= bat.new(:lng,:lng);
barrier (v,i):= language.newRange(0:lng);
	bat.insert(b,i,i);
	redo (v,i):= language.nextElement(1:lng,10:lng);
exit (v,i);
c:= algebra.copy(b);
x:bat[:lng,:lng]:= mal.multiplex("calc","+",b,c);
io.print(x);
