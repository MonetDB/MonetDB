# how does an SQL schema become accessible.

module sql_schema_demo;
	person_key_name := bbp.new(:oid,:str);
	person_key_age  := bbp.new(:oid,:int);
	address_str := bbp.new(:oid,:str);
	address_city:= bbp.new(:oid,:str);
