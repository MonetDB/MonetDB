# A exception in a factory should terminate it 

factory tryme():void;
    bat.setWriteMode(b:bat[:any_1,:any_2]);
barrier always:= true;
    yield always;
    redo always;
exit always;
#catch MALexception;
    #print("exception");
#exit MALexception;
end tryme;
user.tryme();    # shouldn;t hang when you forget the exception
