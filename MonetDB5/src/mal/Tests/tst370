# experimental code section
# permit modules and functions to be designated by function parameters
function z(mod:str,nme:str,title:str):bat;
        b:= mod.nme();
        setName(b,title);
        return b;
end z;

	r:= z("bbp","getNames","names");
	print(r);

