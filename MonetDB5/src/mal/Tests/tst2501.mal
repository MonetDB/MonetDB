# The prototypical factory construct

factory random(seed:int):int;
	i:= seed;
barrier always:= true;
	i:= 125 *i;
	yield i:= i % 32767;
	redo always;
exit always;
end;

r1:= random(1);
print(r1);
r2:= random(1);
print(r2);
