command Admin.lookup(nme:str):bat[:str,:str] 
address CMDlookup; 
function sample(nme:str, val:any):bit;
   b := lookup(nme); #find a bat
   h := select(b,val,val);
   t := count(h);
   x := print(t);
   y := print(val);
end sample;
sample("hello","world");
sample("hello",1);
