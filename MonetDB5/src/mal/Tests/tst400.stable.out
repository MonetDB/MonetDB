stdout of test 'tst400` in directory 'src/mal` itself:
# Monet Database Server V5.0
# Copyright (c) 1993-2004, CWI. All rights reserved.
# compiled for 32biti686-pc-linux-gnu/32bit.
# config:/ufs/mk/monet5/src/mal/Tests/tst.conf
# dbfarm:/ufs/mk/monet5/Linux/var/MonetDB/dbfarm
# dbname:demo
# Visit http://monetdb.cwi.nl for further information.
#	i:= -1;
#barrier v1:= nextElement(i,0,2);
#	print("do something");
#	redo v1:= nextElement(i,0,2);
#exit	v1;
#	printf("left first loop with %s\n",v1);
#	print("next iteration");
#	i:= -1;
#barrier v2:= nextElement(i,0,5,2);
#	print("do something else");
#	redo v2:= nextElement(i,0,5,2);
#exit	v2;
#
function Admin.main():int;	# 0  (main:int)
barrier (v1,i):= mal.newRange(0);	# 1 RNGnewRange_int (v1:bit)(i:int)<-($3:int) jump 4
    io.print("do something");	# 2 IOprint_val ($4:int)<-($5:str)
    redo (v1,i):= mal.nextElement(1,2);	# 3 RNGnextElement_int (v1:bit)(i:int)<-($6:int)($7:int) jump 2
exit (v1,i);	# 4  (v1:bit)(i:int)
    io.printf("left first loop with %s\n",v1);	# 5 IOprint_formatted_bit ($8:int)<-($9:str)(v1:bit)
    io.print("next iteration");	# 6 IOprint_val ($10:int)<-($11:str)
barrier (v2,i):= mal.newRange(0);	# 7 RNGnewRange_int (v2:bit)(i:int)<-($3:int) jump 10
    io.print("do something else");	# 8 IOprint_val ($13:int)<-($14:str)
    redo (v2,i):= mal.nextElement(2,5);	# 9 RNGnextElement_int (v2:bit)(i:int)<-($7:int)($15:int) jump 8
exit (v2,i);	# 10  (v2:bit)(i:int)
end main;	# 11  
[ "do something" ]
[ "do something" ]
left first loop with false
[ "next iteration" ]
[ "do something else" ]
[ "do something else" ]
[ "do something else" ]
>