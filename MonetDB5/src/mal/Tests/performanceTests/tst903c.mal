# cost of a range split

s2:= new(:lng,:lng);
i:= -1:lng;
barrier (v,i):= newRange(0:lng);
	k:= rand();
	l:= lng(k);
	insert(s2,i,i);
	redo (v,i):= nextElement(1:lng,1000000:lng);
	redo v;
exit (v,i);
z:= rangesplit(s2,10);
print(z);
