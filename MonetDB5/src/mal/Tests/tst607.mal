# using spaces

u:str:=const.take(user);
printf("User bound to %s\n",u);

v:int:= const.take(roomnumber);

v:= 123;
v:= v+1;
const.release(roomnumber,v);
const.release(roomnumber,v); # failure? done twice or implicit take?
