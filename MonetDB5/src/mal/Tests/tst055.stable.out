stdout of test 'tst055` in directory 'src/mal` itself:
# Monet Database Server V5.0
# Copyright (c) 1993-2004, CWI. All rights reserved.
# compiled for 32biti686-pc-linux-gnu/32bit.
# config:/ufs/mk/monet5/src/mal/Tests/tst.conf
# dbfarm:/ufs/mk/monet5/Linux/var/MonetDB/dbfarm
# dbname:demo
# Visit http://monetdb.cwi.nl for further information.
## Nil values
#
#s:str := nil:str;	print(s);
#i:sht := nil:sht;	print(i);
#b:bit := nil:bit;	print(b);
#f:flt := nil:flt;	print(f);
#d:dbl := nil:dbl;	print(d);
#l:lng := nil:lng;	print(l);
#j:int := nil:int;	print(j);
#
function Admin.main():int;	# 0  (main:int)
    s := "nil:str";	# 1  (s:str)<-($2:str)
    io.print(s);	# 2 IOprint_val ($3:int)<-(s:str)
    i := nil:sht;	# 3  (i:sht)<-($5:sht)
    io.print(i);	# 4 IOprint_val ($6:int)<-(i:sht)
    b := nil:bit;	# 5  (b:bit)<-($8:bit)
    io.print(b);	# 6 IOprint_val ($9:int)<-(b:bit)
    f := nil:flt;	# 7  (f:flt)<-($11:flt)
    io.print(f);	# 8 IOprint_val ($12:int)<-(f:flt)
    d := nil:dbl;	# 9  (d:dbl)<-($14:dbl)
    io.print(d);	# 10 IOprint_val ($15:int)<-(d:dbl)
    l := nil:lng;	# 11  (l:lng)<-($17:lng)
    io.print(l);	# 12 IOprint_val ($18:int)<-(l:lng)
    j := nil:int;	# 13  (j:int)<-($20:int)
    io.print(j);	# 14 IOprint_val ($21:int)<-(j:int)
end main;	# 15  
[ nil ]
[ nil ]
[ nil ]
[ nil ]
[ nil ]
[ nil ]
[ nil ]
>