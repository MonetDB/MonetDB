#Parsing properties and keeping them around
# note the use of the first parameter to obtain property set
function newsemijoin(A:bat[:int{sorted=true,dense=true},:any$1], 
		B:bat[:int{sorted=true},:any$2]) :bat[:int{unique=true},:any$1];
end newsemijoin;
z:=new(:int{sorted=true},int);
w:=new(:{sequence=true},int);
w:bat[int,int]:=new(:int,:int);
y:=sort(w:bat[int,int]{sorted=true,dense=true});
print(1);
y:int{sorted=true}:=sort(w);
