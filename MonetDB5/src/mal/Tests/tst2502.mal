# Yield commands may only appear in factories

function random(seed:int):int;
	i:= seed;
barrier always:= true;
	i:= 125 *i;
	i:= i % 32767;
	yield i;
	redo always;
exit always;
end;

r1:= random(0);
print(r1);
r2:= random(0);
print(r2);
