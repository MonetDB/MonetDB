# testing the heuristic rules
# relations r(H,T) and s(H,T)
function qry();
r:= bbp.new(:int,:int);

sc:= algebra.copy(r);
s:= bat.reverse(sc);
a:= algebra.semijoin(r,s);
io.print(a);
s:= bat.reverse(sc);
z:= algebra.select(s,0:int,2:int);
io.print(z);
t:= algebra.semijoin(r,s);
z:= algebra.select(t,0:int,6:int);
io.print(z);


optimizer.setDebug();
optimizer.selectPushdown("user","qry");
end qry;
