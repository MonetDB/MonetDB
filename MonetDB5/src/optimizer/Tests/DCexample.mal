    V7 := bat.new(:oid,:oid);
    V10 := bat.new(:oid,:int);
    V16 := algebra.markH(V7);
    V17 := algebra.join(V16,V7);
    V19 := bat.new(:oid,:oid);
    V22 := bat.new(:oid,:int);
    V23 := algebra.join(V16,V22);
	io.print("done");

optimizer.deadcode();
mdb.list();
