#octopus should not sent the same object twice.
function q();
a:bat[:oid,:int]{rows=1000,hlb=0@0,hub=1000@0}:= bat.new(:oid,:int);
(b:bat[:oid,:int]{hlb=0@0,hub=1000@0}):= bat.partition(a);
b1:= algebra.select(b,0,10);
c1:= algebra.select(b,11,20);

u:= algebra.kunion(b1,c1);
io.print(u);
end q;

optimizer.octopus("user","q");
mdb.list("user","q");
