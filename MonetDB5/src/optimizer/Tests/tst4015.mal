# testing repeatative reverse optimalisation
r:= bbp.new(:int,:int);
bat.insert(r,5:int,1:int);
s:= bat.reverse(r);
t:= bat.reverse(s);
k:=23:int;
io.print(t);
optimizer.setDebug();
optimizer.peephole();
