INCLUDES =  -I../../common -I../../gdk \
        -I../../modules/atoms \
        -I../../mal $(Z_CFLAGS) $(BZ_CFLAGS)

MTSAFE

LIBS = {
	DIR = pkglibdir
	SEP = _
	SOURCES = mal.mx mal_init.mx box.mx multiplex.mx  \
		atoms.mx profiler.mx const.mx bbp.mx io.mx \
		enum.mx  streams.mx tcpip.mx inspect.mx bulk_io.mx \
		thread.mx optimizer.mx clients.mx

	HEADERS = h 
	SCRIPTS = mal mil ""
	LIBS = ../common/libmutils ../common/libstream ../../gdk/libbat \
		$(SOCKET_LIBS) $(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
}
scripts_mal = {
	DIR = sysconfdir/MonetDB/mal
	SOURCES = mal_init.mal tcpip.mal thread.mal io.mal streams.mal \
		bulk_io.mal enum.mal profiler.mal mal.mal \
		multiplex.mal const.mal bbp.mal box.mal inspect.mal \
		optimizer.mal clients.mal
}

EXTRA_DIST_DIR = Tests
