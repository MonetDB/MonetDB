#testing the generic list operation
l:= list.new();
l:= list.append(l,1);
l:= list.append(l,2);
barrier i:int:= list.newIterator(l);
	io.print(i);
	redo i:int:= list.hasMoreElements(l);
exit i;

b:= bat.new(:oid,:oid);
k:= list.new();
k:= list.append(k,b);
