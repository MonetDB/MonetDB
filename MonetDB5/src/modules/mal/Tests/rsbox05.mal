# Simple test that tests whether `unknown atoms' are currectly printed
# as strings

include rsbox;

b := bat.new(:void, :date);
bat.insert(b, nil, "1999-05-02":date);

rs := rsbox.create(1, 't');
resultset.populateColumn(rs, 1, b);

resultset.writeNextPart(rs);
