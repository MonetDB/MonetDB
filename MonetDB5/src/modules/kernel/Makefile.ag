
INCLUDES = $(MONETDB_INCS) \
	-I../../../include/atoms \
	-I../../mal $(HWCOUNTERS_INCS) $(PCL_FLAGS)

MTSAFE

LIBS = {
	DIR = pkglibdir
	SEP = _
	SOURCES = \
		calc.mx bat.mx algebra.mx status.mx unix.mx \
		mapi.mx mmath.mx lock.mx alarm.mx \
		batcalc.mx group.mx aggrX3.mx  algebra2.mx array.mx

	HEADERS = h 
	SCRIPTS = mal mil ""
	LIBS = $(MONETDB_LIBS) -lbat -lstream \
                $(SOCKET_LIBS) $(Z_LIBS) $(BZ_LIBS) \
                $(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
	LDFLAGS = -lm
}
#doc_kernel = {
#	DIR = pkgdatadir/docs/modules/kernel
#	SOURCES = \
#		calc.mx bat.mx algebra.mx status.mx unix.mx \
#		mapi.mx mmath.mx lock.mx alarm.mx \
#		batcalc.mx group.mx aggrX3.mx  algebra2.mx
#}

scripts_mal = {
	DIR = pkglibdir
	SOURCES = calc.mx bat.mx algebra.mx status.mx unix.mx \
		mapi.mx mmath.mx lock.mx alarm.mx \
		batcalc.mx group.mx aggrX3.mx  algebra2.mx array.mx
}


EXTRA_DIST_DIR = Tests
