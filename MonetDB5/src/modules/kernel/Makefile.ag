
INCLUDES = -I../../common -I../../gdk \
	../../../include/atoms \
	-I../../mal 

MTSAFE

LIBS = {
	DIR = pkglibdir
	SEP = _
	SOURCES = calc.mx bat.mx algebra.mx system.mx unix.mx \
		mapi.mx kernel_init.mx mmath.mx lock.mx alarm.mx \
		batcalc.mx group.mx aggrX3.mx 

	HEADERS = h 
	SCRIPTS = mal mil ""
	LIBS = ../common/libmutils ../common/libstream ../../gdk/libbat \
		$(SOCKET_LIBS) $(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
}
scripts_mal = {
	DIR = sysconfdir/MonetDB/kernel
	SOURCES = kernel_init.mal calc.mal bat.mal algebra.mal system.mal \
		unix.mal mapi.mal mmath.mal lock.mal alarm.mal batcalc.mal \
		group.mal aggrX3.mal
}


EXTRA_DIST_DIR = Tests
