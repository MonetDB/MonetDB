
INCLUDES = -I../../common -I../../gdk -I../../mal \
	 $(Z_CFLAGS) $(BZ_CFLAGS) $(HWCOUNTERS_INCS) $(PCL_CFLAGS)
MTSAFE

LIBS = {
	DIR = pkglibdir
	SEP = _
	SOURCES = atoms_init.mx str.mx blob.mx url.mx mtime.mx \
		uchr.mx 

	HEADERS = h 
	SCRIPTS = mal mil ""
	LIBS = ../../common/libstream ../../gdk/libbat \
		$(SOCKET_LIBS) $(Z_LIBS) $(BZ_LIBS) \
		$(MALLOC_LIBS) $(PTHREAD_LIBS) $(DL_LIBS)
}
scripts_mal = {
        DIR = sysconfdir/MonetDB/atoms
        SOURCES = atoms_init.mal str.mal blob.mal url.mal mtime.mal \
		uchr.mal decimal.mal
}

EXTRA_DIST_DIR = Tests
