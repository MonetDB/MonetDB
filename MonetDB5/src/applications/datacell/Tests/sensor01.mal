# the sensor event collector
include sql;
include datacell;

datacell.open();
datacell.receptor("toy","localhost",6060);
datacell.define("toy","color",:int);

datacell.dump();
f:= datacell.getReceptors();
io.print(f);
datacell.start("toy");
datacell.dump();

# it should no receive sensor input from port 6060

#datacell.release("toy");
#datacell.dump();

