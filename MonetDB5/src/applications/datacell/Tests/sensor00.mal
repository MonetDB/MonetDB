# the sensor event collector
include sql;
include datacell;
include dc_compiler;

datacell.open();

datacell.receptor("toy", "localhost", 6060);
datacell.define("toy","color",:int);

datacell.dump();

datacell.drop("toy");
datacell.dump();
datacell.close();

