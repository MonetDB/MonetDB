This directory contains the methods and tools
to produce a quick reference over the modules and to
prepare the web infrastructure for document browsing.

Manual:
The files and scripts to generate a complete
manual for Monet 5 for browsing on the web
and as a heavy weight book.

Overview:
a few scripts to generate a concise documentation.
It is the intermediate step towards to real manual.

QuickReference
It works from the XML file produced using the
operation inspect.manual() and saved in the file
manual.xml, which is massaged by the XSLT script 
into a Latex file for further processing

Assets:
contains bits and pieces that are not covered in the
source tree are collected in the directory. 
