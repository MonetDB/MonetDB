@echo off

if not "%1"=="" goto skip
call %0 -r
goto end

:skip

setlocal

set builddir=@XMONETDB5_BUILD@
set srcdir=@MONETDB5_SOURCE@

REM set PYTHONPATH=%builddir%\src\testing;%srcdir%\src\testing;%builddir%\src\common;%srcdir%\src\mapi\clients\python

REM execute Mapprove.py in the source directory
pushd %srcdir%

call Mapprove.py -5 "--config=%builddir%\conf\monetdb5.conf" "--TSTSRCBASE=%srcdir%" "--TSTTRGBASE=%builddir%" %*

popd
endlocal

:end
