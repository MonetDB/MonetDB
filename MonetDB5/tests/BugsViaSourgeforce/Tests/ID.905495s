#!/bin/sh

NAME="$1"

Mlog   "$MSERVER --dbname=$TSTDB $TSTSRCDIR/$NAME.mal"
eval    $MSERVER --dbname=$TSTDB $TSTSRCDIR/$NAME.mal

Mlog   "$MSERVER --dbname=$TSTDB < $TSTSRCDIR/$NAME.mal"
eval    $MSERVER --dbname=$TSTDB < $TSTSRCDIR/$NAME.mal

