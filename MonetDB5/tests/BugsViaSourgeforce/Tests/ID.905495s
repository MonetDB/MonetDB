#!/bin/sh

NAME="$1"

Mlog   "$MSERVER --dbname=$TSTDB $NAME.mal"
eval    $MSERVER --dbname=$TSTDB $NAME.mal

Mlog   "$MSERVER --dbname=$TSTDB < $NAME.mal"
eval    $MSERVER --dbname=$TSTDB < $NAME.mal

