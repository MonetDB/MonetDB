proc int2lng(str intbat, str lngbat) : void
{
	printf("Destroying %s\n", lngbat);
	destroy(lngbat);
	commit();
	printf("Converting int %s to lng %s\n", intbat, lngbat);
	var i := bat(intbat);
	var l:=[lng](i);
	l.rename(lngbat);
	l.persists(true);
	commit();
}

int2lng("xyz", "xyz_lng");
