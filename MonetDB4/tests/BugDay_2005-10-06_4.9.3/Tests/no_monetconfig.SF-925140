#!/bin/sh

echo "monet_daemon=no" > $TSTTRGBASE/config

Mlog "echo 'loaded(); quit();' \| eval \"Mserver --config=$TSTTRGBASE/config\"" 
      echo 'loaded(); quit();' | eval "Mserver --config=$TSTTRGBASE/config"
