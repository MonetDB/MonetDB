for $e in doc("$TSTTRGDIR/sf0.1.xml")//eNest[@aLevel=7] return
  <parent aUnique1="{$e/@aUnique1}" aLevel="{$e/@aLevel}">
  {
    for $c in $e/eNest[position()=2] return 
    <secondChild aUnique1="{$c/@aUnique1}" aLevel="{$c/@aLevel}">
    </secondChild>
  }
  </parent>
