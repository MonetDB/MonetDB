let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/order
where $a/total > 11000.0
order by zero-or-one($a/total) descending
return
    <Output>
        {$a/@id}
        {$a/order_date}
        {$a/total}
    </Output>
