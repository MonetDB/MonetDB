let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/authors/author
where contains(zero-or-one($a/biography), "hockey")
return
<Output>
    {$a/@id}
</Output>
