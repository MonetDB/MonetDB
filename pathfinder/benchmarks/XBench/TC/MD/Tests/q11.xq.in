let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/article/prolog
where $a/dateline/country="Canada"
order by zero-or-one($a/dateline/date)
return
    <Output>
        {$a/title}
        {$a/dateline/date}
    </Output>
