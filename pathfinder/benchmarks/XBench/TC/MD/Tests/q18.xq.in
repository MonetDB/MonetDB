let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/article
where some $p in $a//p satisfies contains(string($p),"the hockey")
return
    <Output>
        {$a/prolog/title}
        {$a/body/abstract}
    </Output>
