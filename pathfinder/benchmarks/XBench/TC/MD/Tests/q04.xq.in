let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/article[@id="8"]/body/section[@heading="introduction"],
    $p in $doc/article[@id="8"]/body/section[. >> $a][1]
return
    <HeadingOfSection>
        {$p/@heading}
    </HeadingOfSection>
