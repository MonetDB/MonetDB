let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/article/prolog
order by zero-or-one($a/dateline/country)
return
    <Output>
        {$a/title}
        {$a/dateline/country}
    </Output>
