let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/article[@id='7']/epilog/references/a_id,
    $b in $doc/article
where $a = $b/@id
return
    <Output>
        {$b/prolog/title}
    </Output>
