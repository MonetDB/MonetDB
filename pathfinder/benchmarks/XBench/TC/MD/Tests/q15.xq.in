let $doc :=
for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
return doc($d)
for $a in $doc/article/prolog/authors/author
where empty($a/contact/text())
return
    <NoContact>
        {$a/name}
    </NoContact>
