let $doc :=
	for $d in doc("$TSTTRGDIR/load_docs.xml")//doc/text()
	return doc($d)
for $a in $doc/*[@doc_id]
where some $p in $a//paragraph satisfies contains($p, "test pattern")
return distinct-values($a/@doc_id)
