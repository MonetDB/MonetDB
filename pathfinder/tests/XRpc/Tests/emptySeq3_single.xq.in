import module namespace xf = "xrpc-fun" at "$TSTSRCDIR/xrpcfun.xq";

declare option xrpc:bulkrpc "no";

(: last parameter is always empty :)
for $str in ("hello", "world")
    for $i in (<a/>, (), <b/>)
        return execute at {"$HOST:$XRPCPORT"}
            {xf:echoVoidWithParams($str, $i, ())}
