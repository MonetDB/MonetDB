import rpc-module namespace xrpc = "http://www.monetdb.nl/basicfunctions" at "$TSTSRCDIR/basicfunctions.xq";

(: the middle parameter is sometimes empty :)
for $i in (<a/>, (), <b/>)
    return xrpc:echoVoidWithParams("localhost", ("abc", (), "xyz"), $i, 100)
