boolean (())
<>
for $b in (1,0) return if ($b) then for $a in (0,1,2,"foo","",0.0,1.3,boolean(1),boolean(0),<a/>) return boolean($a) else boolean (())
<>
for $b in (1,0) return if ($b) then for $a in (0,1,2,"foo","",0.0,1.3,boolean(1),boolean(0),<a/>) return boolean($a) else boolean ($b)
