module(pathfinder);
module(unix);
# should run if you set TSTTRGDIR to pwd

var path:=getenv("TSTSRCDIR") + "/xml-docs";

var query:="doc(\"" + path + "/escaped-string.xml\")//relativepath/text()";

# data model in tabular representation
#
printf("%s",xquery("seq"            ,query , 0));
