# this is the logrotate configuration for merovingian
@LOGDIR@/merovingian.log {
	su monetdb monetdb
	missingok
	notifempty
	postrotate
		[ -s @RUNDIR@/merovingian.pid ] && kill -HUP $(cat @RUNDIR@/merovingian.pid)
	endscript
}
